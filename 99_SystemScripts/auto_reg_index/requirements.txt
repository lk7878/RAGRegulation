# Python 3.11+
# Install: pip install -r requirements.txt

# LLM clients
anthropic>=0.40.0          # Claude Sonnet/Opus (supports batch API)
openai>=1.50.0             # DeepSeek V3 (OpenAI-compatible API)

# PDF / OCR
pdfplumber>=0.11.0         # Electronic PDF text extraction
pypdf>=4.0.0               # Fallback PDF reader
# magic-pdf (MinerU)       # Install separately via: pip install magic-pdf[full]
# See: https://github.com/opendatalab/MinerU

# Baidu OCR (optional, only if using 百度云 OCR for scanned PDFs)
baidu-aip>=4.16.13

# Data / serialization
pyyaml>=6.0.2
pydantic>=2.9.0            # Schema validation
tenacity>=9.0.0            # Retry logic

# Async / concurrency
httpx>=0.27.0
aiofiles>=24.0.0
asyncio-throttle>=1.0.2

# Utilities
python-dotenv>=1.0.1       # .env loader
rich>=13.9.0               # Pretty console output + progress bars
click>=8.1.7               # CLI framework
tqdm>=4.66.0               # Progress bars

# Hashing / dedup
xxhash>=3.5.0              # Fast content hashing

# Optional (Phase 3 GraphRAG)
# lancedb>=0.13.0
# sentence-transformers>=3.0.0  # bge-m3 local embedding

# GraphRAG + Agent (Phase 3 已启用)
networkx>=3.3                  # 关系图 + Louvain 社区检测
rank-bm25>=0.2.2               # BM25 检索
jieba>=0.42.1                  # 中英文混合分词
langchain>=1.0                 # ReAct agent 框架
langchain-openai>=0.2          # DeepSeek via OpenAI-compatible API
langgraph>=0.2                 # Agent 状态图
# langchain-anthropic>=0.3     # 可选，切 Claude Sonnet provider 时装

# Agent 服务层（HTTP + MCP）
fastapi>=0.115                 # OpenAI 兼容的 HTTP 服务
uvicorn[standard]>=0.30        # ASGI 服务器
sse-starlette>=2.1             # SSE streaming 响应
mcp>=1.0                       # Model Context Protocol（Cascade / Claude Desktop 接入）
