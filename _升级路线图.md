---
type: upgrade_roadmap
created: 2026-04-21
version: v1.0
tags:
  - type/upgrade_roadmap
  - planning/strategic
---

# CcVault 升级路线图

**编写日期**：2026-04-21
**适用版本**：v1.0（1429 notes · 12 tools · 5 workflows）
**目标地平线**：6-12 个月

---

## 🎯 愿景

### From（当前）

```
单人 · Obsidian vault · 1429 notes · BM25 关键词检索
        ↓
GraphRAG 33 社区 · Louvain 聚类 · Sonnet 综述
        ↓
ReAct Agent · 12 tools · CLI/HTTP/MCP 三接入
```

### To（目标）

```
多人协作 · Web + Mobile · 5000+ notes · BM25 + Dense Vector Hybrid
        ↓
GraphRAG 层级社区 · Leiden 聚类 · iterative 综述精炼
        ↓
Hierarchical Agent · write tools · Memory · Reflexion
        ↓
条款级检索 · 差异分析 · 合规性检查 · 观测性完善
```

---

## 📊 8 大升级 Track 总览

| Track | 方向 | 当前成熟度 | 优先级 | 预估投入 |
|---|---|---|---|---|
| **1** | 检索升级（Embedding） | ⭐⭐⭐☆☆ | **P0** | 2-3 周 |
| **2** | GraphRAG 升级 | ⭐⭐⭐☆☆ | **P0** | 2-4 周 |
| **3** | Agent 能力升级 | ⭐⭐⭐⭐☆ | P1 | 3-5 周 |
| **4** | 数据质量 | ⭐⭐⭐⭐☆ | P1 | 1-2 周 |
| **5** | 基础设施 | ⭐⭐⭐☆☆ | P1 | 2-4 周 |
| **6** | 前端/UX | ⭐⭐☆☆☆ | P2 | 4-8 周 |
| **7** | 法规场景 | ⭐⭐☆☆☆ | P2 | 4-8 周 |
| **8** | 协作/安全 | ⭐☆☆☆☆ | P3 | 8-12 周 |

**累计**：6-12 个月（单人串行）/ 3-4 个月（2-3 人并行）

---

## 🚀 Track 1 · 检索升级（Embedding Integration）

### 现状分析

**当前能力**：
- 仅 BM25（jieba 分词 + 1.61 MB pickle 索引）
- 查"乘用车制动系统"能命中，但查"踩刹车时防止侧滑"可能漏掉 ESC/ABS 类法规
- 词形变化（活塞/active）、语义近似（紧急刹车/应急制动）不友好

**痛点案例**：

```
查询："新能源电动车着火怎么办" 
BM25 结果: 只命中含"着火"关键词的 notes
期望结果: 应返回 ECE R100 (热失控)、GB 38031 (动力蓄电池安全) 等
```

---

### 1.1 加 Dense Vector 检索（必做）

**方案**：

| 方案 | 模型 | 成本 | 速度 | 推荐 |
|---|---|---:|---:|---|
| A · 本地 | bge-m3 (1024 维) | 硬件 | 批量编码 ~20 min | ★★★★★ |
| B · 本地 | jina-embeddings-v3 | 硬件 | 慢 | ★★★★☆ |
| C · 云 | DashScope text-embedding-v3 | ¥0.5/M tokens | 快 | ★★★☆☆ |
| D · 云 | OpenAI text-embedding-3-large | $0.13/M tokens | 快 | ★★★☆☆ |

**推荐 A · bge-m3**：
- 多语（中英日韩）
- 支持 dense + sparse + colbert 三种向量
- 完全本地，无调用成本
- 1429 notes 编码估 20 分钟（CPU）/ 3 分钟（GPU）

**实现位置**：`@D:\CcVault\99_SystemScripts\auto_reg_index\embeddings\`

```
embeddings/
├── encoder.py           # bge-m3 wrapper
├── store.py             # FAISS / ChromaDB persistence
└── _build_embeddings.py # 一键构建
```

**新 Agent tool**：`search_regulations_vector(query, topk, region, topic)`

---

### 1.2 Hybrid 检索（BM25 + Vector rerank）

**架构**：

```
query → BM25 top-30 ─┐
                     ├→ RRF (Reciprocal Rank Fusion) → top-10
query → Vector top-30┘           ↓
                         可选 rerank (bge-reranker-v2-m3)
                                 ↓
                            最终 top-5
```

**收益**：
- BM25 保关键词精确匹配（reg_id、编号）
- Vector 保语义近似
- Reranker 对 top-30 做 cross-encoder 精排

**工时**：1 周

---

### 1.3 段落级切块（Chunk-level Retrieval）

**现状**：整条 note 作为一个检索单元（BM25 词频在整个 note 上）

**痛点**：ECE R13 有 200 KB body，查"空气制动"可能被 1% 相关内容拖累

**方案**：

```python
# 按章节 / Annex / paragraph 切块
chunks = [
    {"chunk_id": "ECE R13#2.1", "text": "定义..."},
    {"chunk_id": "ECE R13#5.1.1", "text": "性能要求..."},
    {"chunk_id": "ECE R13#Annex 13", "text": "ABS 性能测试..."},
]
```

**存储**：
- Parent note: `01_Wiki/regulations/ece/ECE R13.md`（不变）
- 切块索引: `.stage6/chunks/` + vector store

**Agent 工具**：`search_chunks(query, ...)` 返回 chunk 级结果 + 父 note 链接

**工时**：1-2 周（含切块规则 + UI 集成）

---

### 1.4 Query 重写（HyDE / Query Expansion）

**方案 A · HyDE (Hypothetical Document Embeddings)**：

```
原 query: "电动车热失控后果"
     ↓ LLM 生成"假设答案"
假设答案: "动力电池热失控会导致起火、爆炸、乘员伤亡..."
     ↓ 用"假设答案"作为 embedding query
更好的语义检索结果
```

**方案 B · Multi-query**：

LLM 把原 query 拆成 3-5 个子 query，各自检索后合并结果。

**适用**：用户问"我想出口日本的电动车要满足什么法规"这种宽泛问题。

**工时**：3-5 天

---

### 1.5 结构化检索（Metadata Filters）

**现状**：Agent 工具已支持 region/topic filter，但粒度粗。

**增强**：
- `publication_date` 范围过滤（"2023 年以后的"）
- `status in [active, under_revision]` 多值
- 全文检索 `filename LIKE '%ECE R48%'`

**实现**：在 Agent tool 里扩展参数。

**工时**：2-3 天

---

### Track 1 总结

| 任务 | 工时 | 成本 | 收益 |
|---|---:|---:|---|
| 1.1 Dense Vector | 3-5 天 | ¥0（本地）| 语义搜索 |
| 1.2 Hybrid | 1 周 | ¥0 | 精确+语义兼得 |
| 1.3 Chunk-level | 1-2 周 | ¥0 | 大 note 不被拖累 |
| 1.4 Query 重写 | 3-5 天 | 低 LLM | 宽泛查询改进 |
| 1.5 Metadata Filter | 2-3 天 | ¥0 | 精细过滤 |

**Track 1 累计**：**2-3 周**

---

## 🕸️ Track 2 · GraphRAG 升级

### 现状分析

**当前实现**：
- 节点：1429 个法规 notes
- 边：3 种（supersedes / equivalent_to / references）
- 社区算法：Louvain（python-louvain）
- 社区数：33
- 社区综述：Claude Opus / DeepSeek 单次生成，800-1500 字

**痛点**：
1. Louvain 随机性高（不同种子跑出社区不同）
2. 边太稀疏（平均每节点 <1 条边），很多节点孤立
3. 综述模板化，"矛盾议题"常写"无显著矛盾"
4. 社区粒度固定，无法 drill-down/up

---

### 2.1 Leiden 替换 Louvain（必做）

**为什么**：
- Leiden 解决 Louvain 的"resolution limit"和"badly connected communities"问题
- 社区更稳定（多次跑结果一致）
- 社区内连通性更强

**实现**：

```python
# pip install leidenalg python-igraph
import leidenalg as la
import igraph as ig

g = ig.Graph.from_networkx(nx_graph)
partition = la.find_partition(g, la.ModularityVertexPartition, seed=42)
```

**替换位置**：`@D:\CcVault\99_SystemScripts\auto_reg_index\_graphrag_communities.py`

**工时**：1-2 天（换库 + 重跑 + 对比效果）

---

### 2.2 增加语义相似度边（Dense 边）

**当前边**：
- `supersedes`（ECE R13H 替代 ECE R13H Rev1）
- `equivalent_to`（GB 4785 ≈ ECE R48）
- `references`（body 里 `[[ECE R48]]` wikilink）

**新增边**：
- **`semantic_similar`**：基于 Track 1 的 vector embedding
  - 余弦相似度 > 0.85 → 加一条边
  - 防过度稠密：每节点最多 10 条最强边

**收益**：
- 孤立节点（当前 ~300 个）被纳入社区
- 同主题 GB 标准自然聚合（即使没有显式引用）

**工时**：2-3 天

---

### 2.3 Multi-resolution 层级社区（Hierarchical Communities）

**痛点**：
- 当前"照明与信号"社区有 262 条 notes，太大
- "certification_admin" 社区只 1 条，太小

**方案**：分辨率参数变化（Leiden 支持）

```python
# 低分辨率：大社区（5-10 个宏观主题）
partition_lvl1 = la.find_partition(g, la.RBConfigurationVertexPartition, 
                                    resolution_parameter=0.5)

# 中分辨率：当前的 33 社区
partition_lvl2 = la.find_partition(g, la.RBConfigurationVertexPartition,
                                    resolution_parameter=1.0)

# 高分辨率：细粒度（80-100 个子社区）
partition_lvl3 = la.find_partition(g, la.RBConfigurationVertexPartition,
                                    resolution_parameter=2.0)
```

**UI**：Agent 工具 `search_communities_graphrag(query, level='l1'|'l2'|'l3')`

**可视化**：Sunburst 图或嵌套列表

**工时**：1-2 周

---

### 2.4 社区综述迭代精炼（Iterative Refinement）

**当前问题**：单次 LLM 生成，"矛盾议题"空洞、跨版本推理浅。

**方案**：多轮 refine

```
Round 1: LLM 看成员列表 → 写初稿
Round 2: LLM 自我批判 → 找出 weakness
Round 3: LLM 针对 weakness 深化每节
Round 4: 最终校对 + 格式化
```

**参考**：Microsoft GraphRAG 论文的 "Summary Refinement"

**成本**：每社区从 1 次 LLM call → 4 次（约 4x 成本）

**工时**：1 周（prompt 工程 + 脚本重构）

**预估成本**：
- 33 社区 × 4 轮 × 4K tokens = 530K tokens
- DeepSeek V3: ¥0.8
- Claude Opus: $3-5

---

### 2.5 社区质量自动评分

**当前**：无评分，无法判断综述好坏。

**方案**：LLM-as-judge

```python
scores = {
    "completeness": 0-5,      # 成员是否全覆盖
    "insight": 0-5,           # 是否有非平凡洞察
    "citation": 0-5,          # wikilink 是否准确
    "contradictions_surface": 0-5,  # 是否真的找到矛盾
}
```

低分的社区加入 audit 队列，人工审 or 重生成。

**工时**：3-5 天

---

### 2.6 社区元信息增强

**当前社区 FM**：
```yaml
community_id: 3
label: "排放限值 / 燃料消耗"
member_count: 18
```

**增强**：
```yaml
community_id: 3
label: "排放限值 / 燃料消耗"
member_count: 18
edge_density: 0.72
core_nodes: [ECE R115, ECE R101, GB 19578-2014]
temporal_span: "1998-2025"
region_mix: {cn: 12, ece: 6}
active_vs_superseded: {active: 15, superseded: 3}
key_transitions: 
  - {from: "GB 17691-2005", to: "GB 17691-2018", year: 2018}
related_communities: [4, 11]
```

**工时**：2-3 天

---

### Track 2 总结

| 任务 | 工时 | 成本 | 收益 |
|---|---:|---:|---|
| 2.1 Leiden | 1-2 天 | ¥0 | 稳定性 +50% |
| 2.2 Semantic 边 | 2-3 天 | ¥0 | 连通性 +80% |
| 2.3 Hierarchical | 1-2 周 | ¥0 | 多粒度支持 |
| 2.4 Iterative | 1 周 | ¥5-30 | 综述质量 +30% |
| 2.5 自动评分 | 3-5 天 | ¥1-3 | 可维护性 |
| 2.6 元信息 | 2-3 天 | ¥0 | 查询丰富度 |

**Track 2 累计**：**2-4 周**

---

## 🤖 Track 3 · Agent 能力升级

### 现状分析

**当前 12 tools**：全部 read-only，单层 ReAct loop。

**痛点**：
- 不能让 Agent 主动写 / 改 notes（安全考虑）
- 复杂多步任务需用户反复 prompt 引导
- 无长期 memory，每次对话独立

---

### 3.1 加写操作工具（带审批）

**场景**：让 Agent 协助 vault 维护，而不仅是查询。

**新增工具**：

| 工具 | 用途 | 危险度 |
|---|---|---|
| `update_fm_field` | 改单字段（如 region、status） | 低 |
| `add_equivalence` | 补等价映射 | 低 |
| `backfill_supersedes` | 从 body 回填 supersedes | 中 |
| `create_audit` | 新建 audit 条目 | 低 |
| `rename_note` | reg_id canonicalize 后重命名 | 高 |

**审批机制**：
- 写操作不直接改文件
- 先写入 `05_Audit/pending_writes/<timestamp>.md`
- 用户审核后 `/approve_writes` workflow 批量应用

**工时**：1-2 周

---

### 3.2 Hierarchical Planner（分层规划）

**当前**：ReAct 一次性决定下一步 tool。

**痛点**：复杂问题（"帮我分析国六排放与欧六的主要差异"）需要：
1. 先找国六社区
2. 找欧六社区
3. 读两边主要法规
4. 对比分析

ReAct 可能漏步骤或绕远路。

**方案**：Plan-and-Execute

```
Planner LLM → 输出 task graph:
  [1] search_communities_graphrag("国六排放")
  [2] search_communities_graphrag("欧六排放")
  [3] read_regulation("GB 17691-2018")
  [4] read_regulation("ECE R49 Rev6 Am5")
  [5] 综合对比写报告

Executor LLM → 按 graph 逐步执行，允许动态调整
```

**架构**：LangGraph 已支持（multi-agent supervisor pattern）

**工时**：1-2 周

---

### 3.3 Memory 机制（跨 session 记忆）

**当前**：每次 REPL 对话独立，无持久化记忆。

**方案 A · Mem0（推荐）**：
- SaaS / 自托管皆可
- 自动抽取事实（"用户关心商用车" / "用户偏好 ECE 法规"）
- 下次对话自动注入

**方案 B · 简单 KV**：
- `agent_memory.jsonl`，按用户+topic 索引
- 每次对话开始 LLM 决定 `recall_memory(user_id, topic)`

**新 tool**：
- `save_memory(key, value)`
- `recall_memory(query)`

**工时**：3-5 天（方案 B）/ 1-2 周（方案 A 集成）

---

### 3.4 Reflexion / Self-critique（自我反思）

**场景**：Agent 答错或漏答时自动发现。

**实现**：

```
Step 1: Agent 给出初始答案
Step 2: 另一 LLM（critic）审查："这答案是否完整？有没有遗漏？"
Step 3: 若 critic 发现问题 → Agent 根据反馈重新查询 → 给出改进答案
Step 4: 反馈写入 memory
```

**成本**：每问 2x-3x tokens，但用户满意度显著提升。

**工时**：1 周

---

### 3.5 多 Agent 协作（Supervisor Pattern）

**场景**：复杂任务分给不同专业 Agent：

- **ResearchAgent**：擅长检索 + 综述
- **AuditAgent**：擅长数据质量检查
- **WriterAgent**：擅长生成主题页 / 报告
- **Supervisor**：任务分配 + 结果聚合

**架构**：LangGraph supervisor pattern

**工时**：2-3 周

---

### 3.6 工具组合 Token 优化

**当前痛点**：每次 tool call 都把全部 12 个 tool schema 塞给 LLM（~2000 tokens）

**优化**：
- 按 query 类型动态筛选 tools（"这问题只需要 stats + search"）
- 首 LLM 调用选 category → 第二 LLM 调用只看该 category 的 tools

**收益**：token 消耗 -30%，延迟 -20%

**工时**：2-3 天

---

### Track 3 总结

| 任务 | 工时 | 成本 | 收益 |
|---|---:|---:|---|
| 3.1 写工具 | 1-2 周 | ¥0 | 可协作维护 |
| 3.2 Hierarchical Planner | 1-2 周 | 2x LLM | 复杂任务质量 |
| 3.3 Memory | 3-5 天 - 2 周 | ¥0-低 | 个性化 |
| 3.4 Reflexion | 1 周 | 2-3x LLM | 答案质量 |
| 3.5 Multi-agent | 2-3 周 | 同上 | 专业化 |
| 3.6 Token 优化 | 2-3 天 | ¥0 | 成本/延迟 |

**Track 3 累计**：**3-5 周**

---

## 🗃️ Track 4 · 数据质量（配合审计报告修）

### 关联

本 Track 与 `@D:\CcVault\_审计报告.md` 的 P0/P1 完全对应。参考审计报告的修复清单。

### 4.1 修 P0 三项（1 周）

- P0.1 backfill supersedes（2-3h）
- P0.2 去重 _dup（半天）
- P0.3 ASEAN region（10 min）

### 4.2 修 P1 七项（2-3 周）

- reg_id canonicalize（20 min）
- 等价映射扩展（1-2 天）
- 低置信度复核（2-3 天）
- ...

### 4.3 数据持续监控

加 `_daily_quality_report.py`：
- 每日自动统计 high/medium/low 比例
- 发现异常（如突然大量 low 出现）→ 告警

**Track 4 累计**：**1-3 周**

---

## 🏗️ Track 5 · 基础设施

### 5.1 Neo4j 持久化（替代 JSON graph）

**当前**：
- `@D:\CcVault\99_SystemScripts\auto_reg_index\.stage5\graph.json` 存全图
- 查询时完全加载进内存

**升级**：Neo4j

- Cypher 查询语言
- 大图不用全加载
- 图可视化生态丰富（Neo4j Bloom）

**工时**：1-2 周

**成本**：Neo4j Community 免费 / AuraDB $65/mo

---

### 5.2 观测性（Langfuse / Helicone）

**当前痛点**：
- 不知道 Agent 平均延迟、成功率
- tool 调用失败无告警
- token 消耗难回溯

**方案**：Langfuse

- 自托管（Docker Compose）or Cloud
- 自动追踪 LangChain / LangGraph 调用
- Dashboard 看 traces、costs、latencies

**工时**：2-3 天（集成 + 部署）

---

### 5.3 增量 ingestion

**当前痛点**：

加 1 份新 PDF →
- `_cluster_topics.py` 全量重跑（几秒）
- `_graphrag_communities.py` 全量重跑（几分钟）
- `_graphrag_summarize.py` 可能重生成 33 社区（几 USD）

**升级**：增量算法

- Topic：只对新 note 分配（简单）
- Graph：add node + compute neighborhood only
- Communities：增量 Leiden（有算法支持）
- Summaries：只重算受影响社区

**工时**：1-2 周

---

### 5.4 单元测试 + CI

**当前**：
- `_canonicalize_reg_id` 有 13 用例
- 其他核心函数无自动化测试

**目标**：
- 覆盖 `obsidian_writer.py` FM 生成
- 覆盖 `_shared.py` FM 加载与 reg_id 匹配
- 覆盖 12 个 Agent tool 的 golden output

**CI**：GitHub Actions / 本地 git hook

**工时**：1 周

---

### 5.5 依赖管理 + 版本锁

**当前**：
- `requirements.txt` 用 `>=` 松散约束
- 多台机器跑起来可能不同行为

**升级**：
- `pyproject.toml` + `uv` 或 `poetry`
- `requirements.lock` 完全锁定
- `.python-version` 固定 Python 版本

**工时**：半天

---

### 5.6 Docker / 容器化

**场景**：方便团队成员快速搭环境 or 部署到服务器。

**产物**：
- `Dockerfile`（Python + deps + Baidu/MinerU/pdfplumber）
- `docker-compose.yml`（+ Neo4j + Langfuse + Redis cache）

**工时**：1 周

---

### Track 5 总结

| 任务 | 工时 | 收益 |
|---|---:|---|
| 5.1 Neo4j | 1-2 周 | 图可扩展 |
| 5.2 Langfuse | 2-3 天 | 可观测 |
| 5.3 增量 ingestion | 1-2 周 | 运维效率 |
| 5.4 测试 + CI | 1 周 | 防回归 |
| 5.5 依赖锁 | 半天 | 重现性 |
| 5.6 Docker | 1 周 | 部署便利 |

**Track 5 累计**：**2-4 周**

---

## 🖼️ Track 6 · 前端与用户体验

### 6.1 Web UI（独立 Chat App）

**场景**：不是每人都装 Obsidian，需要浏览器访问。

**技术栈**：
- Frontend: Next.js + shadcn/ui + Tailwind
- Backend: 复用现有 `_agent_server.py`（OpenAI 兼容）
- Deploy: Vercel / 自托管

**功能**：
- Chat 界面（流式）
- 左侧 topic / community 导航
- 右侧 note 详情 preview
- 搜索历史

**工时**：3-4 周

---

### 6.2 Graph 可视化（Interactive Graph Explorer）

**技术**：
- D3.js / Cytoscape.js / react-force-graph
- Backend：`_build_graph.py` 产生的 graph.json

**功能**：
- 按 region / topic 过滤节点
- 点击节点 → 展开邻居
- 社区用颜色区分
- 时间轴滑块（按 publication_date 过滤）

**工时**：2-3 周

---

### 6.3 Mobile 查询 App

**场景**：出差时手机查法规。

**方案 A · PWA**：Web UI 加 PWA manifest，可"加到主屏幕"

**方案 B · React Native**：原生 App，调 HTTP API

**工时**：2-4 周

---

### 6.4 OCR 拍照入库（Mobile）

**场景**：开会拿到纸质法规，拍照直接入库。

**方案**：
- Mobile App 拍照 → 上传到服务器
- Baidu OCR 识别 → 进 pipeline
- 完成后推送通知

**工时**：2-3 周

---

### 6.5 VS Code / Cursor 插件

**场景**：开发者在 IDE 里随手查法规。

**实现**：
- VS Code Extension
- 右键 "查 CcVault"
- 调 HTTP API

**工时**：1-2 周

---

### Track 6 总结

| 任务 | 工时 | 优先级 |
|---|---:|---|
| 6.1 Web UI | 3-4 周 | 中 |
| 6.2 Graph 可视化 | 2-3 周 | 中 |
| 6.3 Mobile | 2-4 周 | 低 |
| 6.4 拍照入库 | 2-3 周 | 低 |
| 6.5 IDE 插件 | 1-2 周 | 低 |

**Track 6 累计**：**4-8 周**（选做）

---

## 📜 Track 7 · 法规场景特定能力

### 7.1 条款级检索（Paragraph/Clause-level）

**当前**：note 整文级检索。

**痛点**：
- ECE R13 有 200+ KB，只想找"5.1.3.1 节"怎么办
- 差异分析需定位到具体条款号

**方案**：
- s6 切块阶段按条款号切（正则识别 `5.1.1.2` 层级）
- 每条款独立 id：`ECE R13#5.1.1.2`
- 检索+引用都带条款号

**工时**：2-3 周

---

### 7.2 差异分析（Version Diff）

**场景**："ECE R13H Rev3 和 Rev4 主要改了哪里"

**方案**：

```
输入: 两个 reg_id
↓
对比 FM (publication_date, supersedes 等)
对比 body (按章节 diff)
LLM 总结"主要变化" 3-5 点
↓
输出:
- 新增条款
- 删除条款
- 修改条款（带逐字 diff）
- 影响评估（哪些产品受影响）
```

**新工具**：`diff_regulations(reg_a, reg_b)`

**工时**：1-2 周

---

### 7.3 合规性检查（车型 → 适用法规）

**场景**："我要出口一款 M1 纯电动车到欧洲，需要满足哪些法规"

**方案**：

```
输入 (车型信息):
- category: M1 (乘用车)
- powertrain: BEV
- target_market: EU

LLM + 多 tool 调用:
1. search "M1 BEV EU type approval"
2. 各主题下 M1 强制要求:
   - 制动: ECE R13H
   - 照明: ECE R48
   - 碰撞: ECE R94, R95, R127
   - 电池: ECE R100, R156
   ...

输出:
- 适用法规清单
- 按生效日排序
- 关联测试方法
- Gap 分析（已有 vs 缺失）
```

**新工具**：`compliance_check(vehicle_profile)`

**工时**：2-3 周（含 meta-schema 设计）

---

### 7.4 生效日时间线（Timeline）

**场景**："未来 12 个月哪些法规会生效/废止"

**UI**：Gantt / Timeline 可视化

**数据**：`implementation_date` / `withdrawal_date` 字段

**工具**：`timeline_upcoming(months=12)`

**工时**：3-5 天

---

### 7.5 法规关系图（Regulation Graph Query）

**查询语言**：Cypher（Neo4j）

**示例**：

```cypher
// 找 GB 4785-2019 的所有祖先版本
MATCH p=(n {reg_id: 'GB 4785-2019'})-[:SUPERSEDES*]->(a)
RETURN p

// 找同时对标 R48 和 R98 的 GB 标准
MATCH (gb {region: 'cn'})-[:EQUIVALENT_TO]->(ece1 {reg_id: 'ECE R48'})
MATCH (gb)-[:EQUIVALENT_TO]->(ece2 {reg_id: 'ECE R98'})
RETURN gb
```

**工具**：`graph_query(cypher_query)`

**工时**：1 周（依赖 Track 5.1 Neo4j）

---

### Track 7 总结

| 任务 | 工时 | 价值 |
|---|---:|---|
| 7.1 条款级检索 | 2-3 周 | ⭐⭐⭐⭐⭐ |
| 7.2 差异分析 | 1-2 周 | ⭐⭐⭐⭐⭐ |
| 7.3 合规性检查 | 2-3 周 | ⭐⭐⭐⭐⭐ |
| 7.4 时间线 | 3-5 天 | ⭐⭐⭐☆☆ |
| 7.5 Graph query | 1 周 | ⭐⭐⭐⭐☆ |

**Track 7 累计**：**4-8 周**

---

## 🔐 Track 8 · 协作与安全

### 8.1 多用户（RBAC）

**当前**：vault 单用户本地。

**需求驱动**：当团队 3+ 人需要共同维护时启动。

**方案**：
- Backend: FastAPI + JWT + PostgreSQL
- Frontend: Web UI（Track 6.1）+ 登录
- RBAC 角色：reader / contributor / admin

**工时**：4-6 周

---

### 8.2 Git-like 分支与 PR

**场景**：多人编辑不冲突。

**方案**：
- 每人自己 branch 改 notes
- 提 PR 时自动跑 `_weekly_check` 做 quality gate
- admin 审核合并

**工时**：2-3 周（依赖 8.1）

---

### 8.3 敏感数据脱敏

**场景**：部分法规可能含内部草案、未公开信息。

**方案**：
- FM 加 `confidentiality: public|internal|confidential`
- Agent 只查用户有权访问的
- Export 时自动脱敏

**工时**：1-2 周

---

### 8.4 审计日志

**场景**：谁改了什么、何时、为什么。

**方案**：
- 每次写操作落 `05_Audit/audit_log.jsonl`
- Web UI 有 "History" 标签

**工时**：3-5 天

---

### 8.5 API 速率限制 / 配额

**场景**：HTTP 服务暴露给外部时防滥用。

**方案**：
- FastAPI middleware
- Redis 存配额
- 按 user / IP 限流

**工时**：2-3 天

---

### Track 8 总结

| 任务 | 工时 | 触发条件 |
|---|---:|---|
| 8.1 多用户 | 4-6 周 | 3+ 人协作时 |
| 8.2 PR 工作流 | 2-3 周 | 多用户后 |
| 8.3 敏感数据 | 1-2 周 | 有机密法规时 |
| 8.4 审计日志 | 3-5 天 | 监管要求时 |
| 8.5 API 限流 | 2-3 天 | 对外开放时 |

**Track 8 累计**：**8-12 周**（按需）

---

## 💰 成本估算汇总

### 一次性开发成本（人力）

假设 1 个高级工程师（¥1500/天）：

| Track | 工时 | 人力成本 |
|---|---:|---:|
| 1. 检索升级 | 2-3 周 | ¥15-22.5k |
| 2. GraphRAG | 2-4 周 | ¥15-30k |
| 3. Agent | 3-5 周 | ¥22.5-37.5k |
| 4. 数据质量 | 1-3 周 | ¥7.5-22.5k |
| 5. 基础设施 | 2-4 周 | ¥15-30k |
| 6. 前端 UX | 4-8 周 | ¥30-60k（选做）|
| 7. 场景能力 | 4-8 周 | ¥30-60k |
| 8. 协作安全 | 8-12 周 | ¥60-90k（选做）|

**核心 Track 1-5**：**10-19 周**，约 **¥75-142.5k**

**全量 8 Track**：**26-47 周**，约 **¥195-352.5k**

### 运行成本（月）

| 项目 | 估算 |
|---|---:|
| LLM API（DeepSeek 日常） | ¥50-200 |
| LLM API（Claude 批量） | ¥100-500（按需 ingestion）|
| 百度 OCR | ¥20-100（新 PDF 量）|
| 云服务（Neo4j AuraDB + Langfuse + Vercel） | ¥500-1000 |
| 硬件电费（本地跑 bge-m3） | ¥20-50 |

**月运行成本**：**¥700-1850**（小团队）

---

## 🗓️ 推荐里程碑

### Q1（Month 1-3）· 核心修复 + 检索升级

**目标**：数据质量达标 + 语义搜索上线

- M1: Track 4 数据质量（修完审计 P0+P1）
- M2: Track 1 检索升级（BM25 + Dense + Hybrid）
- M3: Track 2 GraphRAG 升级（Leiden + Iterative）

**交付**：
- ✅ 无 _dup / 无命名异常
- ✅ Hybrid 检索工具上线
- ✅ 33 社区综述质量 +30%

---

### Q2（Month 4-6）· Agent + 基础设施

**目标**：Agent 能力倍增 + 运维自动化

- M4: Track 3 Agent（写工具 + Memory）
- M5: Track 5 基础设施（Neo4j + Langfuse + 测试）
- M6: Track 7.1 + 7.2（条款级检索 + 差异分析）

**交付**：
- ✅ Agent 可协助 vault 维护
- ✅ Neo4j 持久化
- ✅ 条款级检索可用

---

### Q3（Month 7-9）· 前端 + 场景

**目标**：从 CLI 走向 Web

- M7: Track 6.1 Web UI
- M8: Track 6.2 Graph 可视化
- M9: Track 7.3 合规性检查

**交付**：
- ✅ 浏览器访问 CcVault
- ✅ 图谱可视化
- ✅ 一键查"M1 BEV 出口欧盟需要什么"

---

### Q4（Month 10-12）· 协作 + 打磨

**目标**：按需扩展多用户/Mobile/IDE

- M10+: Track 8 协作（按需）
- M10+: Track 6.3/6.4/6.5 Mobile/IDE（按需）
- M12: 全量 regression + 文档更新

---

## 🎯 优先级建议（如果只能做 3 项）

**Top 3 必做（ROI 最高）**：

1. **Track 1.1 + 1.2 · Hybrid 检索**
   - 工时 1-2 周
   - 立竿见影，检索质量质变

2. **Track 2.1 + 2.4 · Leiden + Iterative 综述**
   - 工时 1-2 周
   - GraphRAG 核心质量提升

3. **Track 4 · P0 修复**
   - 工时 1 周
   - 解决当下用户可见的问题

**共 3-5 周，ROI 最高**。

---

## 🔗 相关文档

- `@D:\CcVault\_完整手册.md` — 知识库完整操作手册（现状全景）
- `@D:\CcVault\_审计报告.md` — 问题清单（与 Track 4 对应）
- `@D:\CcVault\_使用说明.md` — 用户手册
- `@D:\CcVault\CLAUDE.md` — AI Agent 规则
- `@D:\CcVault\_INDEX.md` — 全局主索引

---

**最后更新**：2026-04-21
**维护者**：Cascade
**版本**：v1.0

**下次审视建议**：完成 Track 1 后（预计 2026-05-15）
