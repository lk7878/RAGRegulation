---
type: user_guide
updated: 2026-04-19
tags:
- type/guide
---

# CcVault 使用说明

> 全球汽车法规知识库 · 用户手册
> 基于 [Karpathy LLM Wiki 模式](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) + audit 闭环

---

## 📚 相关文档

| 文档 | 用途 |
|---|---|
| `@D:\CcVault\_INDEX.md` | 全局主索引（单一真源导航） |
| `@D:\CcVault\CLAUDE.md` | Agent 操作手册（Cascade / Claude Code / Cursor 等通用） |
| `@D:\CcVault\_CHANGELOG.md` | 变更日志 |
| `@D:\CcVault\README.md` | 项目概览 |
| `@D:\CcVault\99_SystemScripts\auto_reg_index\_MAINTENANCE.md` | 维护指南（含 Task Scheduler 自动化） |

---

## 一、30 秒快速入门

### 3 个入口

| 需求 | 打开 |
|---|---|
| 了解某技术域全貌 | `04_Topics/_Topics MOC.md` |
| 找中国 GB ↔ ECE ↔ EU 对应 | `03_Equivalence/_Equivalence MOC.md` |
| 看最近动态 / 需复核 / 统计 | `00_Dashboards/_Dashboards MOC.md` |

### 3 个操作

| 需求 | 操作 |
|---|---|
| 找具体法规（知编号） | `Ctrl+O` 输入 `GB 4785` / `R48` |
| 按意图模糊搜 | 命令行 `python _semantic_search.py "国六 轻型车"` |
| 看关系网 | Obsidian 按 `Ctrl+G` |

### 5 个 Agent Workflow（在 Cascade 里用 `/slash` 调用）

| 命令 | 作用 |
|---|---|
| `/ingest` | 投入新 PDF 后完整跑 pipeline + 变化报告 |
| `/add_note` | 手动新增一条法规 note（含双向链/主题页/等价映射） |
| `/fix_classification` | 修正分类错误（单条或批量，含规则修正） |
| `/weekly_check` | 每周健康巡检（数据质量 / 一致性 / 索引时效） |
| `/process_audits` | 批量处理 `05_Audit/` 下人工反馈（闭环纠错） |

Agent 运营手册见 `@D:\CcVault\CLAUDE.md`。

### Audit 闭环（阅读时发现错误？）

1. 在 Obsidian 阅读 note 时发现错 → 按 **`Alt+N`** → 选 `audit` 模板
   - 需装 Templater 插件，配置见 `@D:\CcVault\02_Schema\06_audit_templater_setup.md`
2. 依次选 severity / category / brief → 写 2-3 句 Issue + Expected → 保存
3. 累积 N 条后说 `/process_audits` 让 agent 批量修复

### 数字一览
- **1429** 条汽车法规 notes
- **62** 条跨区域等价映射
- **37** 个技术主题
- **10** 个活数据面板
- **87.4%** 已验证（high/medium confidence）
- 总建设成本 **¥102**（$14.33）

---

## 二、Dataview 插件安装（必装）

**没装 Dataview 就看不到 dashboards 的活数据**。所有 `00_Dashboards/` 下的面板都依赖它。

### 安装步骤

1. Obsidian 设置 → Community plugins
2. 如果第一次用 → Turn on community plugins
3. Browse → 搜 `Dataview` → Install → Enable
4. Enable **JavaScript queries**（重要！部分 dashboard 用 DQL JS）

### 验证

打开 `00_Dashboards/_Needs_Review.md`，如果能看到表格列出低置信度 notes，就是成功。如果看到原始代码 `\`\`\`dataview ...\`\`\``，说明未装成功或未启用 JavaScript queries。

---

## 三、目录结构全景

```
D:\CcVault\
├── 00_Dashboards\          10 个 Dataview 活面板
├── 00_Raw\标准库\          源 PDF 只读备份（OCR 入口）
├── 01_Wiki\regulations\    ★ 核心资产：1429 条汽车法规 notes
│   ├── cn\   462 条 GB / GB/T
│   ├── ece\  959 条 UN / ECE
│   └── eu\ us\ jp\ kr\ au\ br\ asean\ gcc\ ru-eaeu\ in\ cl\ za\ th\
├── 02_Schema\              Schema 权威文档
│   ├── DESIGN.md                     完整设计
│   ├── 02_taxonomy.md                主题/区域 taxonomy
│   ├── 03_frontmatter_schema.md      FM 字段定义（权威！）
│   ├── 04_self_check_rules.md        数据自检规则
│   ├── 06_audit_templater_setup.md   Templater 配置指南
│   └── templates/                    Templater 可调用的模板
├── 02_Wiki\                非法规命名空间（15 条已剥离）
│   ├── non_automotive\     9 条（自行车/塔吊/工业仪器/消防水带）
│   └── references\         6 条（书籍/内部文档）
├── 03_Equivalence\         62 条 GB ↔ ECE/EU/ISO 映射 + MOC
├── 04_Topics\              37 个主题索引页 + MOC
├── 05_Audit\               ★ Audit 闭环：人工反馈条目 + MOC
├── 99_SystemScripts\
│   └── auto_reg_index\     Python pipeline (30+ 脚本)
├── .windsurf\workflows\    5 个 Agent Workflow
├── README.md
├── _INDEX.md               ★ 全局主索引
├── _使用说明.md            ★ 本文件
├── _CHANGELOG.md           变更日志
└── CLAUDE.md               Agent 操作手册
```

---

## 四、5 大日常使用场景

### 场景 1：随手查一条法规

**方法 A**（知道编号）：`Ctrl+O` → 输入 `GB 4785` → 选第一个
**方法 B**（模糊）：命令行 `python _semantic_search.py "前照灯 LED"`

### 场景 2：对比中欧美同一领域

1. 打开 `04_Topics/_Topics MOC.md` → 选主题（例：lighting_signaling）
2. 看"跨区域速查"段，直接看到 CN/ECE/EU/US 最新版本对应
3. 需要更多时点 Dashboards 里的 `_Cross_Region_Matrix.md`

### 场景 3：评估某条 GB 是否采自 ECE

打开 `03_Equivalence/_Equivalence MOC.md`，搜 GB 编号，看 relation：
- `equivalent` — 等效
- `adopts_from` — 采用但有差异
- `aligned_with` — 对标
- `partial` — 部分对应

### 场景 4：跟踪某技术域演进

打开 `00_Dashboards/_Supersession_Chains.md` 看替代关系链。
或主题页下方的"替代链"段。

### 场景 5：查找最新修订

打开 `00_Dashboards/_Recent_Amendments.md`，按 publication_date 倒序展示最近 3 个月的 Amendment。
或 `_By_Region_Latest.md` 按区域看最近 20 条。

---

## 五、法规 note 的结构

每条 note 都有 **20+ 字段 FM + 结构化正文**。

### FM 速览（权威见 `@D:\CcVault\02_Schema\03_frontmatter_schema.md`）

```yaml
---
reg_id: GB 4785-2019              # 规范化唯一编号
title: 汽车及挂车外部照明...       # 中文/本地语
title_en: Prescription for ...     # 英文（若有）
type: version                      # regulation / version / amendment
region: cn                         # cn/ece/eu/us/jp/kr/...
status: active                     # active/superseded/under_revision/draft/withdrawn
publication_date: 2019-05-14
implementation_date: 2020-01-01
supersedes: [GB 4785-2007]
superseded_by: []
equivalent_to:
  - ref: ECE R48 Rev6
    relation: equivalent
    source: "04_Topics/lighting_signaling"
summary: 规定 M/N/O/L 类车辆...
scope: 适用于 ...
keywords: [外部照明, 前照灯]
cross_check_overall_confidence: high
cross_check_flags: []
tags:
  - reg/cn
  - type/version
  - status/verified
  - topic/lighting_signaling
---
```

### 正文结构

- `## 摘要` — 2-3 句总结
- `## 范围` — 适用范围
- `## 关键要求` — 主要技术要求
- `## 试验与验证` — 测试方法（若有）
- `## 参考` — 外链法规

---

## 六、10 个 Dashboard 详解

位置：`00_Dashboards/`

| # | Dashboard | 用途 |
|---|---|---|
| 1 | `_Needs_Review.md` | **低置信度复核清单**（最常用）|
| 2 | `_By_Region_Latest.md` | 各区域最近 20 条 |
| 3 | `_Emissions_Watch.md` | 排放法规跟踪（国六 / Euro 7 / EPA） |
| 4 | `_EV_BEV_Watch.md` | 新能源 / 电池法规 |
| 5 | `_Supersession_Chains.md` | 替代链可视化 |
| 6 | `_Cross_Region_Matrix.md` | 跨区域对标矩阵 |
| 7 | `_Recent_Amendments.md` | 最近 3 个月 Amendment |
| 8 | `_High_Confidence_Index.md` | 高质量索引（建模用）|
| 9 | `_Graph_Insights.md` | 关系图静态分析（PageRank/社区）|
| 10 | `_Semantic_Search.md` | BM25 搜索说明 |

### 优先级使用建议

- **每日**：_Needs_Review / _By_Region_Latest
- **每周**：_Recent_Amendments / _Emissions_Watch（视关注领域）
- **按需**：其他

---

## 七、37 个技术主题

每个主题都是一个独立 Obsidian note，位于 `04_Topics/<key>.md`。结构：

1. **主题综述** — 2-4 段（领域简介 + 国际格局 + 中国情况）
2. **跨区域速查** — 4 行小表格（CN/ECE/EU/US 最新版）
3. **完整索引** — 按 region 分组，按 publication_date 倒序
4. **替代链**（若有）
5. **等价映射**（若有）

主要主题（>50 条）：
- lighting_signaling, emissions, safety_crash, brakes, chassis_suspension, electrical, body_interior, noise_vibration, fuel_system, tyres_wheels

详见 `04_Topics/_Topics MOC.md`。

---

## 八、62 条跨区域等价映射

文件：`03_Equivalence/_Equivalence MOC.md`

### 映射关系类型

| relation | 含义 |
|---|---|
| `equivalent` | 等效采用（技术内容一致） |
| `adopts_from` | 采用但有差异（国情修改） |
| `aligned_with` | 对标（目标一致但条款可能不同） |
| `partial` | 部分对应（一条对多条） |

### 自动化维护

脚本 `_extract_topic_equivalences.py` 从主题页提取映射，`_apply_equivalences_to_notes.py` 回写到 note FM 的 `equivalent_to` 字段。

---

## 九、BM25 语义搜索

### 用法

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index

# 基本搜索
.\.venv\Scripts\python.exe _semantic_search.py "前照灯 LED"

# 限定主题
.\.venv\Scripts\python.exe _semantic_search.py "EMC" --topic electrical

# 限定区域 + 高置信度
.\.venv\Scripts\python.exe _semantic_search.py "排放" --region cn --min-confidence high

# 重建索引（手改 note 后）
.\.venv\Scripts\python.exe _semantic_search.py --rebuild
```

### 参数

| 参数 | 说明 |
|---|---|
| `--topic <key>` | 仅在某主题内搜（见 taxonomy） |
| `--region <code>` | 仅在某区域内搜 |
| `--min-confidence <level>` | high/medium/low |
| `--limit N` | 结果数量（默认 20） |
| `--rebuild` | 重建索引 |

### 评分

BM25 + jieba 中文分词。字段权重：`reg_id×3 + title×3 + title_en×2 + 其他×1`。

---

## 九之二、GraphRAG 层级检索

### 适用场景

**领域型查询**：如"乘用车制动系统整体格局"、"照明认证的中欧对标"。BM25 只能返回散点 notes，但 GraphRAG 能先命中**相关社区**（领域全景），再在社区内做细粒度。

### 用法

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index

# 基本用法
.\.venv\Scripts\python.exe _graphrag_search.py "乘用车制动系统要求"

# 调节返回社区/成员数
.\.venv\Scripts\python.exe _graphrag_search.py "LED 前照灯" -C 3 -N 5

# 按区域过滤社区
.\.venv\Scripts\python.exe _graphrag_search.py "车身结构" --region cn
```

### 输出结构

```
[1] Community #009  制动系统 / 车速表 / 采标关系
    score=9.94 | 6 成员 / 6 边 | ece/brakes
    核心：[[ECE R13-H]], [[GB 21670-2008]]
    摘要：本社区包含 6 个成员...
    → 完整综述：04_Topics/communities/community_009.md
    社区内 top-5 相关法规：
      - GB 21670-2008 (cn) score=28.4  乘用车制动系统...
      - ...
```

### 33 个社区一览

打开 `04_Topics/communities/_Communities MOC.md`，Dataview 自动列出所有社区（按成员数降序）。

### 重建社区（新 PDF 入库后）

```powershell
# 完整重跑（图 → 社区 → LLM 综述）
.\.venv\Scripts\python.exe stages\s5_graphrag.py

# 只重跑 LLM 综述（图和社区不变）
.\.venv\Scripts\python.exe stages\s5_graphrag.py --skip-build --skip-communities --force
```

**成本**：33 社区全量生成 **$0.065** · **5 分 38 秒**（DeepSeek V3，5 线程并发）。

---

## 九之三、ReAct Agent（自然语言问任何问题）

### 适用场景

当你的问题**同时涉及检索 + 统计 + 多步推理**时，单一工具不够用。比如：

- "vault 里有多少条中国法规？其中 brakes 主题多少条？"（需要组合两个计数）
- "国六排放相关的社区是哪个？该社区有多少中国法规？"（需要先社区检索再计数）
- "GB 4785 有哪些历史版本？最新版是哪一版？"（需要沿版本链追溯）

这些问题 Copilot/BM25/GraphRAG 单独都答不好，**ReAct Agent 能自主调用 12 个工具链式推理**解决。

### 用法

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index

# 单次问答
.\.venv\Scripts\python.exe _agent_chat.py "vault 里有多少条中国法规"

# REPL 连续对话（推荐日常使用）
.\.venv\Scripts\python.exe _agent_chat.py
# REPL 内命令：/exit  /clear  /verbose  /help

# 显示 tool 调用轨迹（调试/好奇）
.\.venv\Scripts\python.exe _agent_chat.py -v "..."

# 用 Claude 而不是 DeepSeek
.\.venv\Scripts\python.exe _agent_chat.py --provider claude "..."
```

### 12 个工具一览

| 类别 | 工具 | 用途 |
|---|---|---|
| 检索 | `search_regulations_bm25` | BM25 点查 |
| 检索 | `search_communities_graphrag` | 社区层级检索 |
| 检索 | `read_regulation` | 按 reg_id 读详情 |
| 统计 | `describe_vault` | 全局概览 |
| 统计 | `count_regulations` | 条件计数 |
| 统计 | `stats_by_field` | 分组统计 |
| 统计 | `list_recent_changes` | 最近改动 |
| 结构 | `get_equivalence` | 等价映射 |
| 结构 | `get_supersession_chain` | 版本链 |
| 结构 | `get_community` | 社区详情 |
| 结构 | `list_by_topic` | 主题成员 |
| 审计 | `list_needs_review` | 待复核 notes |

### 成本

| 问题复杂度 | 调用数 | 耗时 | 成本 |
|---|---:|---:|---:|
| 简单统计 | 2 | 18s | ≈ $0.001 |
| 中等组合 | 4 | 30s | ≈ $0.0025 |
| 复杂推理 | 10 | 53s | ≈ $0.007 |

**100 次日常查询约 ¥1.5**（DeepSeek V3 默认 provider）。

### 何时用 Agent 何时用别的

| 场景 | 推荐 |
|---|---|
| 已知 reg_id / 精确找某条 | BM25 (`_semantic_search.py`) |
| 问"某领域法规有哪些" | GraphRAG (`_graphrag_search.py`) |
| **混合/统计/多步推理** | **Agent (`_agent_chat.py`)** |
| FM 精确字段筛选 | Dataview 面板 |

详见 `99_SystemScripts/auto_reg_index/agent/README.md`。

### Agent 可以在 Obsidian Copilot 里直接用（HTTP 服务）

不想每次切终端？启动一个 HTTP 服务，在 Obsidian Copilot 聊天面板里直接用：

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _agent_server.py
```

然后 Obsidian Copilot Settings → Model Settings → Add Custom Model：
- **Model Name**: `ccvault-agent`
- **Base URL**: `http://127.0.0.1:7777/v1`
- **API Key**: `dummy`
- **Provider**: `OpenAI Format`

切换到 `ccvault-agent` 模型即可在 Obsidian 里自然对话。

### Agent 可以让其他 AI（Cascade / Claude Desktop / Cursor）调用（MCP）

把 12 个工具通过 MCP 协议暴露出去，其他 AI IDE 里就能用。

**Cascade（Windsurf）配置**：编辑 `%USERPROFILE%\.codeium\windsurf\mcp_config.json`：

```json
{
  "mcpServers": {
    "ccvault": {
      "command": "D:/CcVault/99_SystemScripts/auto_reg_index/.venv/Scripts/python.exe",
      "args": ["D:/CcVault/99_SystemScripts/auto_reg_index/_agent_mcp.py"]
    }
  }
}
```

重启 Cascade，以后让它维护 vault 不用再 `read_file` 摸索，它可以直接 `describe_vault()` 秒回。

---

## 十、新 PDF 入库

### 放 PDF
放到 `D:\CcVault\00_Raw\标准库\<region>\`

### 一键跑（★ 推荐）
```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _daily_maintenance.py
```

**这一条命令** = OCR + Extract + Write + 质量复核 + 导航层更新 + 索引重建 + 变化报告。

详细用法见 `@D:\CcVault\99_SystemScripts\auto_reg_index\_MAINTENANCE.md`（含 Windows Task Scheduler 自动化教程）。

### 模式选择

| 场景 | 命令 | 耗时 |
|---|---|---:|
| 完整跑（有新 PDF） | `_daily_maintenance.py` | 10-60 分钟 |
| 只刷新索引（手改了 note） | `_daily_maintenance.py --only-index` | ~25 秒 |
| 跳过 LLM（只跑规则） | `_daily_maintenance.py --skip-llm` | ~1-2 分钟 |
| 干跑检查（不执行） | `_daily_maintenance.py --dry-run` | 即时 |

### Agent Workflow 方式（更简单）

在 Cascade 里直接说：
```
/ingest
```

会自动跑 dry-run → 确认 → 执行 → 变化报告。

### 成本
| 规模 | 成本 |
|---|---:|
| 10 条 | ~¥0.7 |
| 100 条 | ~¥7 |
| 1000 条 | ~¥70 |

---

## 十一、Audit 人工反馈闭环

阅读 notes 时发现机器没抓到的问题？用 audit 机制**不打断阅读**地累积反馈。

### 完整流程

```
阅读 note 时发现错
   ↓
Alt+N → 选 audit 模板
   ↓
选 Severity (critical/high/medium/low)
   ↓
选 Category (accuracy/completeness/classification/link/formatting/other)
   ↓
输入 Brief (文件名用)
   ↓
在 ## Issue 段写 2-5 句描述问题
   ↓
在 ## Expected 段写期望
   ↓
保存 → 回到原 note 继续读
   ↓
累积到若干条后，对 Cascade 说 /process_audits
   ↓
Agent 批量处理 → 改 note → 标 resolved → 报告
```

### 严重度约定

| 级别 | 含义 |
|---|---|
| `critical` | 数据错误影响决策 |
| `high` | 主要字段错误 |
| `medium` | 摘要或内容错 |
| `low` | 格式或可读性 |

### 配置教程

`@D:\CcVault\02_Schema\06_audit_templater_setup.md`（5 步，10 分钟一次性）。

### 面板

`@D:\CcVault\05_Audit\_Audit MOC.md` — 待处理/resolved 统计 + 按 reg_id 聚合。

### 机器 vs 人工反馈

| 渠道 | 触发方 | 用途 |
|---|---|---|
| `status/needs-review` tag | 机器（置信度算法） | 自动发现低置信度 |
| `cross_check_flags` 字段 | 机器（LLM 校对） | 自动发现元数据异常 |
| `05_Audit/` | **人工** | 阅读时发现的问题 |

三位一体覆盖所有质量问题类型。

---

## 十二、数据质量

### 当前指标（2026-04）

| 指标 | 数值 | 目标 |
|---|---:|---|
| regulations total | 1429 | - |
| high confidence | 70.4% | ≥70% ✓ |
| medium confidence | 22.9% | - |
| low confidence | 6.0% | ≤10% ✓ |
| unknown confidence | 0.6% | ≤1% ✓ |
| verified tag | 87.4% | ≥85% ✓ |

### 自检

```powershell
# 整体质量
.\.venv\Scripts\python.exe _stage2_stats.py

# 全量检查（耗时）
.\.venv\Scripts\python.exe _qc_full.py
```

或直接在 Cascade 里 `/weekly_check` 获得 12 项体检报告。

---

## 十三、FAQ

### Q1：我该装哪些 Obsidian 插件？

**必装**：
- Dataview（看 dashboards）
- Templater（用 Alt+N 建 audit）

**建议装**：
- Graph Analysis（关系图增强）
- Recent Files（最近打开）

**不建议装**：
- Kanban / Calendar（和本库场景不符）

### Q2：文件目录 `00_` / `01_` 这些前缀能改吗？

**不要改**。前缀保证 Obsidian 文件树按字母序显示时符合心智顺序。改了会：
- 砸碎所有 Dataview 查询（硬编码路径）
- 砸碎 pipeline 脚本（WIKI 路径常量）

### Q3：我改了一条 note，搜索搜不到？

BM25 索引过期。跑：
```powershell
.\.venv\Scripts\python.exe _semantic_search.py --rebuild
```
或 `_daily_maintenance.py --only-index`（25 秒）。

### Q4：新法规 PDF 我放哪里？

放到 `D:\CcVault\00_Raw\标准库\<region>\` 下对应区域文件夹。

不要直接在 `01_Wiki/regulations/` 下建 md —— 除非走 `/add_note` workflow。

### Q5：置信度低的 notes 怎么处理？

打开 `00_Dashboards/_Needs_Review.md`，列表里的每条 note：
- 读 cross_check_flags 字段看机器发现了什么问题
- 手工复核正确 → 改 FM 的 `cross_check_overall_confidence: high` + `status/verified` tag
- 发现错误 → 建 audit 或直接改

### Q6：我想新增一个主题怎么办？

1. 改 `_cluster_topics.py` 的 TOPICS 字典添加新主题 key + 规则
2. 跑 `_cluster_topics.py` + `_write_topic_pages.py`
3. 主题 MOC 自动更新
4. 如果是分类错误批量修复，用 `/fix_classification` workflow

### Q7：手误删了文件怎么办？

- **Obsidian 内删除**：先去 `.trash/` 找（需在设置里启用"Move to Obsidian trash"）
- **系统删除**：Windows 回收站

**防护建议**（立即配）：
- Obsidian 设置 → Files & links → **Deleted files** → 选 **"Move to Obsidian trash (.trash folder)"**
- 定期复制 `D:\CcVault\` 到外部硬盘

### Q8：Dataview 面板显示错误？

1. 检查插件启用
2. 启用 JavaScript queries
3. 检查 DQL 里的 FROM 路径是否为 `"01_Wiki/regulations"`（引号 + 无前导斜杠）

### Q9：想导出成 PDF / Word？

Obsidian 原生支持 Markdown → PDF（File → Export to PDF）。多文件批量用 pandoc。

### Q10：能加密存储吗？

整个 vault 可以放 VeraCrypt / BitLocker 加密卷里。Obsidian 无感工作。

### Q11：一次性处理多少条 audit 合理？

- 10-30 条：Cascade 5-15 分钟跑完
- 50+ 条：考虑先 `/process_audits --dry-run` 看影响面

### Q12：Pipeline 跑失败怎么办？

1. 看 `99_SystemScripts/auto_reg_index/logs/maintenance_<ts>.log` 末尾报错
2. 常见：
   - OCR 失败 → 检查 Baidu OCR 免费额度或 PDF 是否损坏
   - LLM API 失败 → 10 分钟后重试；检查 `.env` key
   - 某脚本卡死 → Ctrl+C 中断，单步重跑

---

## 十四、故障排查

### 问题 1：Obsidian 加载慢

**原因**：Dataview 查询多 / 关系图大。
**解决**：关闭 Graph view；只在需要时打开 Dashboards。

### 问题 2：搜不到我知道存在的法规

**原因**：BM25 索引过期。
**解决**：`_daily_maintenance.py --only-index`。

### 问题 3：note 里的 wikilink 是红色的

**原因**：链接目标文件不存在或被重命名。
**解决**：
- `/weekly_check` 会报告所有 broken wikilinks
- 手动在 Obsidian `Ctrl+Shift+F` 全局搜 + 替换

### 问题 4：`status: superseded` 但没 `superseded_by`

**原因**：替代链未正确建立。
**解决**：
```powershell
.\.venv\Scripts\python.exe _build_supersession_chain.py
```

### 问题 5：新建 note 的 FM 不完整

**原因**：手工建的没按 schema 填。
**解决**：走 `/add_note` workflow（agent 会自动补齐）。

### 问题 6：DeepSeek API 报 rate limit

**原因**：并发过高。
**解决**：降 `PIPELINE_CONCURRENCY`（`.env` 里）从 10 改到 5。

### 问题 7：磁盘空间不够

**检查**：
- `99_SystemScripts/auto_reg_index/.staging/` — OCR 中间产物可清理
- `99_SystemScripts/auto_reg_index/logs/` — 超 30 天 log 可清理
- `.stage5/` — 索引缓存可重建

---

## 附录 A：脚本速查

位置：`99_SystemScripts/auto_reg_index/`

### 最常用
- `_daily_maintenance.py` — ★ 一键维护
- `_semantic_search.py` — BM25 检索
- `ingest.py` — 完整 pipeline CLI

### 数据建设
- `stages/s0_ocr.py` — OCR（pdfplumber + Baidu + MinerU）
- `stages/s1_extract.py` — DeepSeek 提取结构化 FM
- `_backfill_titles.py` — 补 FM 缺失字段
- `_run_cross_check.py` — 质量复核
- `_reclassify_false_mismatches.py` — 降级假告警

### 导航层
- `_cluster_topics.py` — 主题聚类
- `_write_topic_pages.py` — 主题页生成
- `_extract_topic_equivalences.py` — 等价映射提取
- `_write_equivalence_page.py` — MOC 生成
- `_apply_equivalences_to_notes.py` — 回写 FM
- `_build_supersession_chain.py` — 替代链

### Stage 5
- `_build_graph.py` — networkx GraphML
- `_graph_analytics.py` — PageRank / Betweenness

### 辅助
- `_stage2_stats.py` — 置信度分布
- `_topic_digest.py` — 主题摘要
- `_qc_full.py` — 全量质量检查
- `_manifest_sync.py` — manifest 一致性
- `_fix_existing_notes.py` — 批量修复
- `_migrate_non_automotive.py` — 迁移非汽车标准

---

## 附录 B：Dataview 查询示例

### 按区域统计

```dataview
TABLE length(rows) AS "Count"
FROM "01_Wiki/regulations"
WHERE region
GROUP BY region
SORT length(rows) DESC
```

### 最近 3 个月的 amendments

```dataview
TABLE title, region, publication_date
FROM "01_Wiki/regulations"
WHERE type = "amendment" AND publication_date >= date(today) - dur(3 months)
SORT publication_date DESC
```

### 低置信度 notes

```dataview
TABLE reg_id, title, cross_check_overall_confidence
FROM "01_Wiki/regulations"
WHERE cross_check_overall_confidence = "low"
SORT file.name ASC
```

### 我关注主题的最新版本

```dataview
TABLE reg_id, title, publication_date
FROM "01_Wiki/regulations"
WHERE contains(tags, "topic/lighting_signaling") AND status = "active"
SORT publication_date DESC
LIMIT 10
```

---

## 附录 C：术语表

| 术语 | 含义 |
|---|---|
| **reg_id** | 规范化法规唯一编号（如 `GB 4785-2019` / `UN R094 Rev4 Am1`） |
| **FM** | Frontmatter（note 文件顶部的 YAML 元数据） |
| **MOC** | Map of Content（Obsidian 社区术语：导航索引页） |
| **BM25** | 一种基于词频的经典检索算法（非向量） |
| **Dataview** | Obsidian 插件，把 FM 当数据库查询 |
| **Templater** | Obsidian 插件，支持脚本化模板 |
| **Supersession** | 替代关系（老版本被新版本替代） |
| **Equivalence** | 等价关系（跨区域对应） |
| **Topic** | 技术主题（37 个之一） |
| **Cross-check** | 跨源质量复核（LLM 对比 PDF 原文 vs FM） |
| **Confidence** | 置信度（high/medium/low/unknown） |
| **Audit** | 人工发现的错误/改进反馈条目 |
| **Pipeline** | 端到端数据处理流水线（OCR → Extract → Write → QC → Index） |

---

## 附录 D：Pipeline 架构

```
┌────────────────────────────────────────────────────────┐
│ 00_Raw/标准库/<region>/*.pdf                           │
└──────────────────┬─────────────────────────────────────┘
                   │ S0: OCR (pdfplumber → Baidu → MinerU)
                   ↓
┌────────────────────────────────────────────────────────┐
│ .staging/ocr/*.txt                                     │
└──────────────────┬─────────────────────────────────────┘
                   │ S1: Extract (DeepSeek V3)
                   ↓
┌────────────────────────────────────────────────────────┐
│ .staging/extract/*.yaml (FM) + *.md (body)            │
└──────────────────┬─────────────────────────────────────┘
                   │ S2: Write (合并 + 规范化)
                   ↓
┌────────────────────────────────────────────────────────┐
│ 01_Wiki/regulations/<region>/<reg_id>.md              │
└──────────────────┬─────────────────────────────────────┘
                   │ S3: Quality (cross_check + flags)
                   │ S4: Topics (cluster + topic pages + equivalence)
                   │ S5: Indices (BM25 + Graph)
                   ↓
┌────────────────────────────────────────────────────────┐
│ 04_Topics/ + 03_Equivalence/ + .stage5/bm25_index.pkl │
│ + .stage5/graph.graphml                                │
└────────────────────────────────────────────────────────┘
```

**各阶段脚本对应**：
- S0 → `stages/s0_ocr.py`
- S1 → `stages/s1_extract.py`
- S2 → `_backfill_titles.py` 等
- S3 → `_run_cross_check.py` + `_reclassify_false_mismatches.py`
- S4 → `_cluster_topics.py` → `_write_topic_pages.py` → `_extract/write/apply_equivalences.py`
- S5 → `_build_graph.py` + `_graph_analytics.py` + `_semantic_search.py --rebuild`

**一键跑全部**：`_daily_maintenance.py`

---

**最后更新**：2026-04-19
**维护者**：Cascade (agent) + 用户
**参考**：Karpathy LLM Wiki + lewislulu/llm-wiki-skill audit 模式
