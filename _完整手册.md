---
type: master_manual
updated: 2026-04-21
tags:
  - type/manual
  - scope/everything
---

# CcVault 完整操作手册

> 本文档是 **CcVault 全部能力 + 每个功能的演示示例** 的单一入口。
>
> 受众：用户本人（忘记怎么做时查） + 未来的 AI agent（读完即上手）。
>
> 其他文档：`@D:\CcVault\CLAUDE.md`（agent 规则） · `@D:\CcVault\_使用说明.md`（用户指南） · `@D:\CcVault\_INDEX.md`（全局地图）。

---

## 目录

- [第 0 章 · 5 分钟速览](#第-0-章--5-分钟速览)
- [第 1 章 · 架构全景](#第-1-章--架构全景)
- [第 2 章 · 核心概念（数据模型）](#第-2-章--核心概念数据模型)
- [第 3 章 · 12 个检索查询工具](#第-3-章--12-个检索查询工具)
- [第 4 章 · Agent 对话（三种接入方式）](#第-4-章--agent-对话三种接入方式)
- [第 5 章 · 维护运营（5 个 Workflow + 诊断工具）](#第-5-章--维护运营5-个-workflow--诊断工具)
- [第 6 章 · Pipeline 内部（6 个 Stage）](#第-6-章--pipeline-内部6-个-stage)
- [第 7 章 · MOC 导航层](#第-7-章--moc-导航层)
- [第 8 章 · Dashboards（10 个活面板）](#第-8-章--dashboards10-个活面板)
- [第 9 章 · 故障排查](#第-9-章--故障排查)
- [第 10 章 · 开发扩展](#第-10-章--开发扩展)
- [附录 A · Frontmatter 字段完整表](#附录-a--frontmatter-字段完整表)
- [附录 B · 37 个 Topic 全表](#附录-b--37-个-topic-全表)
- [附录 C · 16 个 Region 全表](#附录-c--16-个-region-全表)
- [附录 D · 12 个 Agent 工具 API 参考](#附录-d--12-个-agent-工具-api-参考)
- [附录 E · 50+ 脚本清单](#附录-e--50-脚本清单)
- [附录 F · 命令速查卡](#附录-f--命令速查卡)

---

# 第 0 章 · 5 分钟速览

## 这是什么

**CcVault** = 全球汽车法规结构化知识库。

| 指标                    |                                                   数值 |
| --------------------- | ---------------------------------------------------: |
| 法规 notes 总数           |                                            **1,429** |
| 覆盖区域数                 | **16**（ECE / CN / EU / US / JP / KR / AU / BR / ...） |
| 技术主题数                 |                                               **37** |
| GraphRAG 社区数          |                                               **33** |
| 跨区域等价映射               |                                               **62** |
| 数据质量（high confidence） |                                                70.8% |
|                       |                                                      |

## 3 大能力

1. **查询检索**（12 个工具）—— 关键词 / 主题 / 区域 / 社区 / 版本链 / 等价关系任意组合
2. **自然语言对话**（ReAct Agent）—— 用人话问任何问题，agent 自主调工具回答
3. **维护运营**（5 个 Workflow）—— 新 PDF 入库、修正分类、审计处理、健康巡检

## 30 秒上手

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index

# 最常用：自然语言问一切
.\.venv\Scripts\python.exe _agent_chat.py "vault 里有多少条 ECE 法规？"

# 关键词搜索
.\.venv\Scripts\python.exe _semantic_search.py "前照灯 LED"

# 启动 HTTP 服务（给 Obsidian Copilot 用）
.\.venv\Scripts\python.exe _agent_server.py
```

## 架构一图

```
┌─────────────────────────────────────────────┐
│       用户 / Agent / Obsidian Copilot       │
└──────┬──────────┬──────────────┬────────────┘
       │          │              │
  CLI  │  HTTP :7777 (OpenAI API)  │  MCP stdio
       ▼          ▼              ▼
    ┌────────────────────────────────┐
    │  ReAct Agent (LangGraph)       │
    │  12 tools · DeepSeek V3        │
    └──────────────┬─────────────────┘
                   ▼
    ┌────────────────────────────────┐
    │  vault 数据层                  │
    │  01_Wiki/ (1429 notes)         │
    │  + BM25 索引                   │
    │  + .stage4/ 聚类 + 关系图      │
    │  + 04_Topics/communities/ 33 个│
    └──────────────▲─────────────────┘
                   │
    ┌──────────────┴─────────────────┐
    │  Pipeline (6 stages s0-s5)     │
    │  OCR → Extract → Cross-check → │
    │  Equivalence → Topic → GraphRAG│
    └────────────────────────────────┘
```

---

# 第 1 章 · 架构全景

## 1.1 目录结构

```
D:\CcVault\
├── 00_Dashboards\          10 个 Dataview 活面板
├── 00_Raw\标准库\          源 PDF 只读备份（OCR 入口）
├── 01_Wiki\regulations\    【★ 核心资产】1429 条 notes
│   ├── cn\   448 条 GB / GB/T
│   ├── ece\  959 条 UN / ECE
│   ├── eu\ us\ jp\ kr\ au\ br\ asean\ gcc\ ru-eaeu\ in\ cl\ za\ th\
├── 02_Schema\              ★ Schema 权威文档
│   ├── DESIGN.md                     完整设计
│   ├── 02_taxonomy.md                主题/区域 taxonomy
│   ├── 03_frontmatter_schema.md      FM 字段定义
│   ├── 04_self_check_rules.md        数据自检规则
│   ├── 06_audit_templater_setup.md
│   ├── 07_obsidian_copilot_setup.md
│   └── templates\                    Templater 模板（8 种节点）
├── 02_Wiki\                非法规命名空间（15 条已剥离）
├── 03_Equivalence\         62 条 GB ↔ ECE/EU/ISO 映射 MOC
├── 04_Topics\              37 个主题索引页 + 33 GraphRAG 社区
│   └── communities\        33 个社区综述
├── 05_Audit\               人工审计队列
├── 99_SystemScripts\auto_reg_index\  Python pipeline + Agent 层
│   ├── stages\             s0-s5 六个阶段
│   ├── writers\            obsidian_writer.py
│   ├── agent\              ReAct Agent（tools / server / mcp_server）
│   ├── ocr\ llm\ prompts\  OCR + LLM + Prompt 模板
│   ├── .stage3\.stage4\.stage5\  中间产物
│   └── logs\ cache\        日志 + 缓存
├── .windsurf\workflows\    5 个 Workflow
├── CLAUDE.md               ★ AI Agent 规则
├── README.md               项目概览
├── _INDEX.md               ★ 全局主索引
├── _使用说明.md            ★ 用户手册（14 章）
├── _CHANGELOG.md           人读版变更日志
└── _完整手册.md            ★ 本文件
```

## 1.2 数据流

```
[源 PDF]  00_Raw/标准库/<region>/*.pdf
   │
   ▼ stages/s0_ocr.py              (pdfplumber / baidu / mineru)
[OCR 文本]  .staging/<reg_id>/ocr.md
   │
   ▼ stages/s1_extract.py          (DeepSeek V3)
[结构化 JSON]  .staging/<reg_id>/extracted.json
   │
   ▼ stages/s2_cross_check.py      (Sonnet batch)
[校对结果]  confidence 字段落实
   │
   ▼ writers/obsidian_writer.py
[Obsidian notes]  01_Wiki/regulations/<region>/<reg_id>.md
   │
   ▼ stages/s3_equivalence.py      (Opus batch)
[跨区等价]  03_Equivalence/ + FM.equivalent_to
   │
   ▼ stages/s4_topic_summary.py    (Sonnet batch)
[主题索引]  04_Topics/*.md
   │
   ▼ stages/s5_graphrag.py         (Louvain + Opus)
[社区综述]  04_Topics/communities/community_*.md
   │
   ▼ _semantic_search.py --rebuild
[BM25 索引]  .stage5/bm25_index.pkl
   │
   ▼ _build_graph.py + _graph_analytics.py
[关系图]  .stage5/graph.json + graph.graphml
```

## 1.3 技术栈

| 层 | 技术 |
|---|---|
| 存储 | Obsidian vault（纯 markdown + YAML frontmatter） |
| OCR | pdfplumber（电子 PDF） + Baidu OCR（扫描件） + MinerU（复杂表格） |
| LLM 抽取 | DeepSeek V3（便宜批量） |
| LLM 校对 | Claude Sonnet 4.6（batch API） |
| LLM 跨区等价 | Claude Opus 4.7（batch API） |
| LLM 社区综述 | DeepSeek V3 / Claude Opus 4.7 |
| Agent 框架 | LangGraph ReAct |
| 检索引擎 | rank_bm25 + jieba（中英文混合分词） |
| 图算法 | NetworkX + python-louvain |
| HTTP 服务 | FastAPI + uvicorn |
| MCP 服务 | mcp Python SDK (stdio) |
| 客户端 | Obsidian Copilot · Cascade (Windsurf) · Claude Desktop |

## 1.4 成本概览

日常查询（Agent 对话）：**$0.001-0.007 / 次**。

---

# 第 2 章 · 核心概念（数据模型）

## 2.1 reg_id 命名规范

`reg_id` 是每条 note 的**唯一身份证**。规范化规则由 `_canonicalize_reg_id` 函数实现（位于 `@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s1_extract.py:321`）。

### ECE / UN 法规

| 输入形式 | 规范化后 |
|---|---|
| `R094r4am1e`（UN 文件名 stem） | `ECE R94 Rev4 Am1` |
| `R088am1e` | `ECE R88 Am1` |
| `R013Hr4am1e` | `ECE R13H Rev4 Am1` |
| `ECE R008r4e` | `ECE R8 Rev4` |
| `UN R094r4am1e` | `ECE R94 Rev4 Am1` |
| `UN R13H Rev4 Am4` | `ECE R13H Rev4 Am4` |
| `ECE R13-H` | `ECE R13H` |
| `ECE-R13-H` | `ECE R13H` |

**H 后缀**：仅 `ECE R13H` 一条实例（Harmonized passenger cars braking），但正则泛化以支持未来变体。

### GB / GB/T 中国国标

| 形式 | 规范化后 |
|---|---|
| `GB 4785-2019` | `GB 4785-2019`（保持） |
| `GB/T 12534-1990` | `GB/T 12534-1990`（保持） |
| `4785-2019-gb` | `GB 4785-2019` |

### 演示：测试 reg_id 归一化

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe -c "from stages.s1_extract import _canonicalize_reg_id; print(_canonicalize_reg_id('R013Hr4am1e'))"
# 输出: ECE R13H Rev4 Am1
```

## 2.2 Frontmatter Schema

**权威定义**：`@D:\CcVault\02_Schema\03_frontmatter_schema.md`

### 必填字段

```yaml
---
reg_id: GB 4785-2019
title: 汽车及挂车外部照明和光信号装置的安装规定
region: cn                   # cn/ece/eu/us/jp/kr/au/br/ru-eaeu/...
status: active               # active/superseded/withdrawn/draft/under_revision/...
type: version                # regulation/version/amendment
---
```

### 强烈建议字段

```yaml
title_en: Prescription for installation of lighting ...
publication_date: 2019-05-14
implementation_date: 2020-01-01
source_pdf: _ingest/cn/GB 4785-2019.pdf
cross_check_overall_confidence: high   # high/medium/low/unknown
cross_check_flags: []
```

### 关系字段

```yaml
supersedes:                  # 手工填
  - GB 4785-2007
superseded_by: []            # 脚本自动反向生成（手工不填）
equivalent_to:               # 脚本自动从主题页提取
  - ref: ECE R48 Rev6
    relation: equivalent     # equivalent/adopts_from/aligned_with/partial
    source: "04_Topics/lighting_signaling"
```

### 内容字段

```yaml
summary: 规定 M/N/O/L 类车辆外部照明灯具安装要求。
scope: 适用于所有在中国境内销售的机动车辆。
keywords: [外部照明, 前照灯, 转向信号灯]
```

### 分类标签

```yaml
tags:
  - reg/cn                   # reg/<region>
  - type/version             # type/<type>
  - status/verified          # status/verified | status/needs-review
  - topic/lighting_signaling # topic/<key>
```

### 演示：读一条真实 note

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "读一下 GB 4785-2019"
```

**预期输出**：

```
=== GB 4785-2019 ===
title       : 汽车及挂车外部照明和光信号装置的安装规定
title_en    : [空]
region      : cn
status      : active
topic       : lighting_signaling
publication : 2019-12-31
effective   :
confidence  : high
path        : D:\CcVault\01_Wiki\regulations\cn\GB 4785-2019.md
equivalent_to: ["{'ref': 'ECE R48', 'relation': 'equivalent', ...}"]
```

## 2.3 主题体系（37 个 topic）

- 定义位置：`@D:\CcVault\04_Topics\`（每个 topic 一个索引页）
- 聚类规则：`@D:\CcVault\99_SystemScripts\auto_reg_index\_cluster_topics.py`
- 真相存放：`@D:\CcVault\99_SystemScripts\auto_reg_index\.stage4\cluster_assignment.json`（**不在** note FM 的 `topic` 字段）

**top 10 主题**（完整见 [附录 B](#附录-b--37-个-topic-全表)）：

| 主题 | 数量 | 典型法规 |
|---|---:|---|
| `lighting_signaling` | 262 | ECE R48, R112, R7, R13H, GB 4785 |
| `tires_wheels` | 115 | ECE R30, R54, R124, GB 9743 |
| `emissions_exhaust` | 100 | ECE R49, R83, GB 17691, GB 18352 |
| `crash_impact` | 89 | ECE R94, R95, R137, FMVSS 208 |
| `brakes` | 84 | ECE R13, R13H, R90, GB 12676 |
| `restraints_airbags` | 78 | ECE R16, R44, R129, GB 14167 |
| `noise` | 68 | ECE R51, R41, GB 1495 |
| `special_vehicles` | 55 | ECE R105, ADR 42, GB 7258 |
| `visibility_glazing` | 53 | ECE R43, ECE R46, GB 9656 |
| `anti_theft_security` | 53 | ECE R116, GB 15740 |

## 2.4 区域（region）

**16 个 region**（见 [附录 C](#附录-c--16-个-region-全表)）：

| region | 数量 |
|---|---:|
| `ece` | 959 |
| `cn` | 448 |
| `eu` | 5 |
| `ru-eaeu` | 3 |
| `cl`, `us` | 2 each |
| `au`, `br`, `gcc`, `id`, `jp`, `kr`, `my`, `sa`, `th`, `za` | 1 each |

## 2.5 状态（status）

| status | 含义 |
|---|---|
| `active` | 现行有效 |
| `superseded` | 已被新版替代 |
| `under_revision` | 修订中 |
| `draft` | 草案 / 征求意见稿 |
| `withdrawn` | 已废止 |
| `original` | 初版（历史） |
| `unknown` | 未知（建议复核） |

## 2.6 置信度（confidence）

由 `s2_cross_check.py` 产生：

| confidence | 含义 | 目标 |
|---|---|---:|
| `high` | 标题 + 日期 + 范围都吻合 | ≥ 70% |
| `medium` | 轻微不一致但无关键错误 | - |
| `low` | 关键字段存疑 | ≤ 10% |
| `unknown` | 校对未完成 | ≤ 1% |

**当前分布**（2026-04）：`high 70.8% · medium 22.9% · low 5.7% · unknown 0.6%`

## 2.7 关系字段

### supersedes / superseded_by（版本链）

```yaml
# GB 4785-2019 的 FM
supersedes:
  - GB 4785-2007

# GB 4785-2007 的 FM（脚本自动回填）
superseded_by:
  - GB 4785-2019
```

### equivalent_to（跨区域等价）

```yaml
equivalent_to:
  - ref: ECE R48 Rev6
    relation: equivalent   # equivalent/adopts_from/aligned_with/partial
    source: "04_Topics/lighting_signaling"
```

### references（隐式 wikilink）

Body 里 `[[ECE R7]]` 格式的 wikilink，通过 `_build_graph.py` 提取为 references 边。

## 2.8 GraphRAG 社区（33 个）

- **定义**：基于 supersedes / equivalent_to / references 的 **Louvain 聚类**
- **产物**：`@D:\CcVault\04_Topics\communities\community_000.md` 到 `community_032.md`

每个社区文件示例：

```yaml
---
label: 排放限值 / 燃料消耗 / 替代燃料 / 试验方法
member_count: 18
edge_count: 20
top_region: cn
top_topic: emissions_exhaust
core_nodes: [ECE R115, ECE R101, GB 19578-2014]
---

# Community #003

## 1. 总体概述
...

## 2. 内部关系结构
核心网络：[[ECE R115]]（替代燃料加注系统）是整个社区引用关系的枢纽...

## 3. 同类对比
...

## 4. 矛盾议题
...

[[ECE R101]] [[ECE R110]] [[ECE R115]] ...  (18 个 wikilink 成员)
```

---

# 第 3 章 · 12 个检索查询工具

这 **12 个工具**是 CcVault 最核心的查询能力。**所有工具都可以 4 种方式调用**：

1. **Agent 对话**（`_agent_chat.py "自然语言"`）— 最方便
2. **MCP 让 Cascade 直接调**（配置见 4.3）
3. **HTTP 给 Obsidian Copilot 用**（配置见 4.2）
4. **直接 Python import**（`from agent.tools import ALL_TOOLS`）

分 4 类：检索（3）· 统计（4）· 结构（4）· 审计（1）。

## 3.1 `search_regulations_bm25` — BM25 关键词检索

**用途**：按中英文关键词 + 区域 + 主题过滤，找具体某几条法规。

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `query` | str | 必填 | 自然语言查询词 |
| `region` | str | None | 过滤区域：cn/ece/eu/... |
| `topic` | str | None | 过滤主题（37 个 topic 之一） |
| `limit` | int | 10 | 返回前 N 条（最大 30） |

### 演示

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _semantic_search.py "前照灯 LED"
```

**预期输出**：

```
Top 10 BM25 hits for '前照灯 LED':
  12.51  ECE R48 Rev6 Am8        (ece) [lighting_signaling] Uniform provisions concerning...
  11.88  ECE R112                (ece) [lighting_signaling] 关于装用非对称近光和远光灯的...
  10.76  GB 25991-2010           (cn)  [lighting_signaling] 汽车用 LED 前照灯
  10.12  ECE R149                (ece) [lighting_signaling] Road Illumination Devices (RID)
  ...
```

### 演示：带区域过滤

```powershell
.\.venv\Scripts\python.exe _semantic_search.py "EMC" --region cn --min-confidence high
```

### 演示：Agent 调用

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "帮我找中国标准里关于前照灯 LED 的法规"
# Agent 自动调 search_regulations_bm25(query='前照灯 LED', region='cn')
```

---

## 3.2 `search_communities_graphrag` — GraphRAG 层级检索

**用途**：问"某领域的整体格局"时，先命中 top-K 相关社区（领域全景），再在社区成员内细粒度 BM25。

| 参数 | 默认 | 说明 |
|---|---|---|
| `query` | 必填 | 自然语言查询词 |
| `topk_communities` | 3 | 返回前 N 个社区（最大 10） |
| `topk_members` | 5 | 每社区内返回前 M 条法规（最大 15） |

### 演示：查询制动系统全景

```powershell
.\.venv\Scripts\python.exe _graphrag_search.py "乘用车制动系统要求"
```

**预期输出**：

```
Top 3 GraphRAG communities for '乘用车制动系统要求':

[1] Community #011  制动系统 / ESC / ABS / 制动液
    score=0.856 | 14 成员 / 18 边 | ece/brakes
    核心: ECE R13, ECE R13H, ECE R90
    摘要: 乘用车制动体系以 ECE R13H 为核心...
    成员 top-5:
      - ECE R13H              (ece) score=12.01 Uniform provisions concerning...
      - ECE R13               (ece) score=11.20 有关 M、N 和 O 类车辆制动...
      - GB 21670-2008         (cn)  score=9.85  乘用车制动系统技术要求...
      - ECE R90               (ece) score=8.70  Replacement brake lining assemblies
      - GB 13594-2003         (cn)  score=8.20  机动车和挂车防抱制动性能...
```

### 演示：Agent 调用

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "国六排放相关的社区是哪个？这个社区里有多少条中国法规？"
# 4 次 tool 调用，~30 秒
```

---

## 3.3 `read_regulation` — 按 reg_id 读详情

**用途**：已知某条法规的 reg_id，读完整 FM + body 前 N 字符。

| 参数 | 默认 | 说明 |
|---|---|---|
| `reg_id` | 必填 | 如 `GB 4785-2019` 或 `ECE R13H` |
| `body_chars` | 2000 | body 返回多少字符（最大 6000） |

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "读一下 GB 4785-2019 的内容"
```

**预期输出**：

```
=== GB 4785-2019 ===
title       : 汽车及挂车外部照明和光信号装置的安装规定
region      : cn
status      : active
topic       : lighting_signaling
publication : 2019-12-31
confidence  : high
path        : D:\CcVault\01_Wiki\regulations\cn\GB 4785-2019.md
equivalent_to: ["{'ref': 'ECE R48', 'relation': 'equivalent', ...}"]

--- body preview ---
**前言**

本标准按照GB/T 1.1-2009给出的规则起草。
本标准代替GB 4785-2007《汽车及挂车外部照明和光信号装置的安装规定》。
...
```

**容错**：reg_id 匹配 `GB4785-2019` / `gb 4785-2019` / `GB 4785-2019` 都找得到。

---

## 3.4 `describe_vault` — 全局概览

**用途**：问"vault 里有多少 X / 整体情况"首选调用。**无参数**。

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "vault 里有多少法规？"
```

**预期输出**：

```
=== CcVault 全局概览 ===
Notes 总数: 1429
社区数 (GraphRAG): 33
等价映射条数: 95

— 按区域分布 —
  ece       : 959
  cn        : 448
  eu        : 5
  ru-eaeu   : 3
  cl, us    : 2 each
  其他 10 个 region: 1 each

— 按 status 分布 —
  active        : 1399
  superseded    : 15
  unknown       : 9
  under_revision: 2
  withdrawn     : 1

— 按 cross_check 置信度 —
  high      : 1011 (70.8%)
  medium    : 328  (22.9%)
  low       : 81   (5.7%)
  unknown   : 9    (0.6%)

— 主题分布 top-15 —
  lighting_signaling     : 262
  tires_wheels           : 115
  emissions_exhaust      : 100
  crash_impact           : 89
  brakes                 : 84
  ...
```

---

## 3.5 `count_regulations` — 按条件计数

**用途**：精确回答"XX 有多少条"。

| 参数 | 说明 |
|---|---|
| `region` | cn / ece / eu / us / jp / kr / ... |
| `topic` | 37 个 topic 之一 |
| `status` | active / superseded / withdrawn / ... |
| `year_from` | publication_date 年份 >= |
| `year_to` | publication_date 年份 <= |
| `confidence` | high / medium / low |

**全部可选**，不传则对全量计数。

### 演示：多条件组合

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "2024 年后发布的电动车相关中国法规有多少条？"
```

Agent 自动调 `count_regulations(region='cn', topic='hv_battery_ev', year_from=2024)`。

**预期输出**：

```
Matching count: 7  [region=cn AND topic=hv_battery_ev AND year>=2024]

前 7 条 preview:
  GB 38031-2025     (cn) 2025-01-15 [active] 电动汽车用动力蓄电池安全要求...
  GB 18384-2024     (cn) 2024-09-30 [active] 电动汽车安全要求...
  ...
```

---

## 3.6 `stats_by_field` — 按字段分组统计

**用途**：按单字段做分组计数。**支持字段**：`region / topic / status / year / confidence / source_type`

### 演示：每年新增法规

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "按年份看一下法规分布"
```

**预期输出**：

```
=== 按 year 分布（共 62 组，show top 30） ===
  2024         : 52
  2023         : 48
  2022         : 47
  2021         : 55
  2020         : 48
  2019         : 50
  ...
  (unknown)    : 10
```

---

## 3.7 `list_recent_changes` — 最近改动

**用途**：回答"最近改了什么"。优先用 git log，降级 mtime。

| 参数 | 默认 | 说明 |
|---|---|---|
| `days` | 7 | 回溯天数（最大 90） |
| `limit` | 20 | 最多列出几条（最大 50） |

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "最近 7 天改了什么"
```

**预期输出**（有 git）：

```
最近 3 个 commits 涉及 notes 改动:

  2026-04-21  a3f9b2e  fix: R13H 命名规则补全
      + 01_Wiki/regulations/ece/ECE R13H Rev4 Am1.md
      + 01_Wiki/regulations/ece/ECE R13H.md

  2026-04-20  8c1d023  add: GB 38031-2025
      + 01_Wiki/regulations/cn/GB 38031-2025.md
      + 04_Topics/hv_battery_ev - 电动车 · 动力电池 · 充电保护.md
```

---

## 3.8 `get_equivalence` — 跨区等价映射

**用途**："ECE R13 对应的中国标准是什么""GB 4785 和国外对标情况"。

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "GB 4785-2019 对标哪些国外法规？"
```

**预期输出**：

```
=== GB 4785-2019 的跨区域等价映射 ===
  → ECE R48 Rev6           [equivalent] Based on stage3_curated:lighting_signaling
```

---

## 3.9 `get_supersession_chain` — 版本演化链

**用途**："GB 4785 历经了哪些版本""ECE R13 最新版是哪版"。

双向追溯：沿 `supersedes` 向后找最早，沿 `superseded_by` 向前找最新。

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "GB 4785 的历史版本是哪些"
```

**预期输出**（Agent 做多步推理）：

```
=== GB 4785-2019 版本链 ===
更早版本（supersedes 链）:
  ← GB 4785-2007
  ← GB 4785-1998
  ← GB 4785-1984

当前 note: GB 4785-2019  status=active  publication=2019-12-31
```

> **已知限制**：部分老版本的 `supersedes` 字段缺失（body 有"本标准代替"但 FM 没抽取），详见 memory《CcVault 完整审计报告》。

---

## 3.10 `get_community` — 社区详情

**用途**：已知社区编号（0-32），深入看综述 + 成员。

### 演示：看 #3 号排放社区

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "读一下 #3 社区的综述"
```

**预期输出**：

```
=== Community #003 ===
label        : 排放限值 / 燃料消耗 / 替代燃料 / 试验方法
member_count : 18
edge_count   : 20
top_region   : cn
top_topic    : emissions_exhaust
core_nodes   : [ECE R115, ECE R101, GB 19578-2014]

成员 (18):
  - ECE R101
  - ECE R115
  - ECE R49
  - ECE R83
  - GB 17691-2018
  - GB 19578-2024
  ...

--- 综述摘要 ---
## 2. 内部关系结构
社区内部关系呈现一个以 [[ECE R115]] 为核心的技术法规网络...
```

---

## 3.11 `list_by_topic` — 按主题列法规

| 参数 | 默认 | 说明 |
|---|---|---|
| `topic` | 必填 | 37 个 topic 之一 |
| `limit` | 50 | 最多列出（最大 200） |

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "brakes 主题下有哪些法规"
```

**预期输出**（按 region 分组）：

```
=== Topic=brakes: 84 条 notes ===

[cn] 21 条:
  GB 12676-2014    商用车辆和挂车制动系统技术要求及试验方法
  GB 13594-2003    机动车和挂车防抱制动性能和试验方法
  GB 21670-2008    乘用车制动系统技术要求及试验方法
  ...

[ece] 63 条:
  ECE R13          有关 M、N 和 O 类车辆制动认证的统一规定
  ECE R13H         关于乘用车制动认证的统一规定
  ECE R90          Replacement brake lining assemblies
  ...
```

---

## 3.12 `list_needs_review` — 审计队列

**用途**："哪些 notes 需要人工复核""哪些低质量条目"。

| 参数 | 默认 | 说明 |
|---|---|---|
| `limit` | 20 | 最多返回（最大 100） |
| `confidence` | `"low"` | 门限：low / medium / high。传 "medium" 返回 low+medium |

### 演示

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "哪些 notes 需要复核"
```

**预期输出**：

```
需要复核的 notes（阈值 confidence ≤ low）: 共 81 条
  [low] ADR                      (au ) [special_vehicles     ] 澳大利亚汽车设计规则
  [low] ASEAN-UNKNOWN             (ece) [type_approval_general] 东盟汽车市场技术法规和认证
  [low] ECE R116 Am10             (ece) [anti_theft_security  ] Uniform provisions concerning
  [low] ECE R13                   (ece) [brakes               ] Uniform provisions concerning
  [low] ECE R13 Rev4 Am2          (ece) [brakes               ] Uniform provisions concerning
  ...
  ... and 71 more (increase limit to see all)
```

---

# 第 4 章 · Agent 对话（三种接入方式）

第 3 章的 12 个工具既可以**单独运行 Python 脚本**，也可以交给 **ReAct Agent** 让 LLM **自主编排调用**。这层解决了纯 RAG 回答不了统计题/全量扫描题的根本限制。

Agent 实现位于 `@D:\CcVault\99_SystemScripts\auto_reg_index\agent\`：

```
agent/
├── agent.py          build_agent() + run_once() + system_prompt
├── cli.py            argparse + REPL + rich 渲染
├── server.py         FastAPI OpenAI 兼容 server
├── mcp_server.py     MCP Server (stdio)
└── tools/            12 个工具实现
```

**Agent 底座**：LangGraph ReAct，默认 **DeepSeek V3**（$0.27/M in, $1.10/M out，中文好）。

**三种接入方式**：

| 方式 | 入口 | 客户端 | 用途 |
|---|---|---|---|
| **CLI** | `_agent_chat.py` | 终端 | 开发调试、脚本自动化 |
| **HTTP** | `_agent_server.py` | Obsidian Copilot / 任何 OpenAI SDK | 插件集成 |
| **MCP** | `_agent_mcp.py` | Cascade / Claude Desktop / Cursor | IDE 维护 vault |

---

## 4.1 CLI 模式（终端对话）

### 4.1.1 单次问答

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _agent_chat.py "vault 里有多少条中国法规"
```

**预期输出**：

```
vault 里总共有 448 条中国法规（GB / GB/T）。
其中 active 状态 437 条，superseded 8 条，其他 3 条。
```

### 4.1.2 REPL 连续对话

```powershell
.\.venv\Scripts\python.exe _agent_chat.py
```

```
╭─ CcVault Agent REPL (DeepSeek V3)
│  Ctrl-D / 'exit' 退出 · 'clear' 清空上下文
╰─

> vault 里有多少条 ECE 法规？
959 条

> 其中 brakes 主题多少条？
84 条（cn 21 + ece 63；如需要具体清单我可以继续列）

> 列前 5 条
[自动调 list_by_topic]
...
```

### 4.1.3 显示 tool trace（调试用）

```powershell
.\.venv\Scripts\python.exe _agent_chat.py -v "国六排放相关的社区是哪个"
```

**verbose 输出**：

```
╭─ agent.run (DeepSeek V3)
│  Q: 国六排放相关的社区是哪个
╰─

  [tool 1] search_communities_graphrag(query='国六排放', topk_communities=3)
           → Top 3 matches: Community #003, #004, #011

  [tool 2] get_community(community_id=3)
           → Community #003 排放限值 / 燃料消耗 ... (18 成员)

  [tool 3] get_community(community_id=4)
           → Community #004 车辆分类 / 制动 / 尺寸 ...

A: 国六排放核心是 Community #003 "排放限值/燃料消耗"，18 成员...
```

### 4.1.4 切换底层模型

```powershell
# Claude Sonnet（如果有 key）
.\.venv\Scripts\python.exe _agent_chat.py --provider claude "复杂推理问题"

# 指定特定模型
.\.venv\Scripts\python.exe _agent_chat.py --model deepseek-reasoner "..."
```

---

## 4.2 HTTP 模式（Obsidian Copilot 集成）

把 Agent 包一层 FastAPI，暴露 **OpenAI 兼容** 的 `/v1/chat/completions` 接口。

### 4.2.1 启动服务

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index

# 默认本地 7777 端口
.\.venv\Scripts\python.exe _agent_server.py

# 允许局域网访问
.\.venv\Scripts\python.exe _agent_server.py --host 0.0.0.0 --port 7777
```

**预期输出**：

```
╭─ CcVault Agent Server
│  URL:       http://127.0.0.1:7777
│  /v1/models                → 列模型
│  /v1/chat/completions      → OpenAI 兼容聊天接口
│  /health                   → 健康检查
│  /docs                     → Swagger UI
╰─ Obsidian Copilot 配置: base_url=http://127.0.0.1:7777/v1, model=ccvault-agent
INFO:     Started server process [3713]
INFO:     Uvicorn running on http://127.0.0.1:7777
```

### 4.2.2 Obsidian Copilot 配置

**Settings → Copilot → Model Settings → Add Custom Model**：

| 字段 | 值 |
|---|---|
| Model Name | `ccvault-agent` |
| Provider | `OpenAI Format` |
| Base URL | `http://127.0.0.1:7777/v1` |
| API Key | `dummy`（服务端不校验） |
| Model Capabilities | Tool use ✓ |

点 **Verify** → 返回 ✓。

### 4.2.3 使用

Copilot 聊天面板 → 模型下拉选 `ccvault-agent`：

```
> vault 里有多少条 ECE 法规？
→ 959 条（agent 调 describe_vault）

> GB 4785 有哪些历史版本？
→ 1984 → 1998 → 2007 → 2019，最新 GB 4785-2019
```

### 4.2.4 API 请求示例

```powershell
curl -X POST http://127.0.0.1:7777/v1/chat/completions `
  -H "Content-Type: application/json" `
  -d '{
    "model": "ccvault-agent",
    "messages": [{"role": "user", "content": "vault 里有多少条 ECE 法规？"}],
    "stream": false
  }'
```

**响应**：

```json
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "model": "ccvault-agent",
  "choices": [{
    "index": 0,
    "message": {"role": "assistant", "content": "959 条..."},
    "finish_reason": "stop"
  }],
  "usage": {"prompt_tokens": 2030, "completion_tokens": 380, "total_tokens": 2410}
}
```

### 4.2.5 性能说明

- **非流式**（`stream=false`）：一次返回完整答案，简单 18s / 复杂 53s
- **伪流式**（`stream=true`）：Agent 推理完后按 20 字一块 SSE 推送（用户感官渐进）
- **Agent 实例缓存**：首次构建 ~1s，之后请求复用

### 4.2.6 关闭服务

Ctrl-C 或关闭终端。后台进程关闭：

```powershell
netstat -ano | findstr :7777
Stop-Process -Id <PID> -Force
```

---

## 4.3 MCP 模式（Cascade / Claude Desktop / Cursor 集成）

**MCP (Model Context Protocol)** 是 Anthropic 标准，支持 MCP 的 LLM 客户端能**直接调用 CcVault 的 12 个工具**。

### 4.3.1 配置 MCP

编辑 `%USERPROFILE%\.codeium\windsurf\mcp_config.json`（Cascade/Windsurf）：

```json
{
  "mcpServers": {
    "ccvault": {
      "command": "D:/CcVault/99_SystemScripts/auto_reg_index/.venv/Scripts/python.exe",
      "args": ["D:/CcVault/99_SystemScripts/auto_reg_index/_agent_mcp.py"]
    }
  }
}
```

**Claude Desktop**：`%APPDATA%\Claude\claude_desktop_config.json`（Windows）/ `~/Library/Application Support/Claude/claude_desktop_config.json`（macOS）同样结构。

**Cursor**：Settings → MCP → Add new → 粘贴 JSON。

### 4.3.2 重启客户端

完全退出客户端（任务栏托盘右键 Quit），重新打开。

### 4.3.3 验证

在客户端发消息试：

```
> 用 ccvault 的工具查一下 vault 里有多少条中国法规
```

客户端会展示 **tool call 步骤**（"调用 `ccvault.describe_vault()`"），然后给出真实答案。

### 4.3.4 MCP 工具前缀

Cascade 里 12 个工具以 `mcp0_` 前缀暴露：

```
mcp0_describe_vault
mcp0_count_regulations
mcp0_stats_by_field
mcp0_list_recent_changes
mcp0_search_regulations_bm25
mcp0_search_communities_graphrag
mcp0_read_regulation
mcp0_get_equivalence
mcp0_get_supersession_chain
mcp0_get_community
mcp0_list_by_topic
mcp0_list_needs_review
```

### 4.3.5 MCP vs HTTP 对比

| 对比 | HTTP | MCP |
|---|---|---|
| 协议 | REST + SSE | JSON-RPC over stdio |
| 客户端 | Obsidian Copilot / OpenAI SDK | Cascade / Claude Desktop / Cursor |
| 是否常驻 | 手动启动 `_agent_server.py` | 客户端自动 spawn 子进程 |
| 并发 | 多客户端可共享 | 每客户端独立进程 |
| 适用场景 | Obsidian 日常查询 | IDE 里让 AI 帮你维护 vault |

**两套可同时启用**，互不干扰。

---

## 4.4 Agent 能力边界

### ✅ Agent 能做

1. **统计题**（纯 RAG 做不到）—— "vault 里有多少 cn 法规"
2. **多步推理** —— "国六排放社区里有多少中国法规"（4 tool calls）
3. **版本链追溯** —— "GB 4785 历史版本"（多次 supersession 跳跃）
4. **读完整 note** —— body + FM
5. **审计候选生成** —— "哪些 notes 需要复核"

### ❌ Agent 不能做（需专用脚本）

1. **改 vault 内容** —— Agent 层没有写操作（safety）
2. **跑 pipeline** —— 用 `_daily_maintenance.py`
3. **管 git / 文件系统** —— 让 Cascade 直接执行 PowerShell

### 💰 成本参考

| 问题复杂度 | tool 调用数 | 耗时 | tokens | 成本 |
|---|---:|---:|---:|---:|
| 简单统计 | 2 | 18s | 2K in / 0.3K out | ≈ $0.001 |
| 中等（社区组合） | 4 | 30s | 5K in / 0.8K out | ≈ $0.0025 |
| 复杂（版本链追溯） | 10 | 53s | 15K in / 2K out | ≈ $0.007 |

100 次日常查询平均 ≈ **$0.2**（¥1.5）。

---

# 第 5 章 · 维护运营（5 个 Workflow + 诊断工具）

5 个 Workflow 位于 `@D:\CcVault\.windsurf\workflows\`，在 Cascade/Windsurf 里用 `/slash` 调用。其他 agent 可读文件内容作为 prompt。

| 命令 | 场景 | 耗时 |
|---|---|---:|
| `/ingest` | 新 PDF 入库 | 5min–hours |
| `/add_note` | 手工新增法规 | 2-5 min |
| `/fix_classification` | 修正分类错误 | 1-5 min |
| `/weekly_check` | 每周健康巡检 | 2-5 min |
| `/process_audits` | 批量处理人工反馈 | 5-30 min |

---

## 5.1 `/ingest` — 新 PDF 入库

**完整定义**：`@D:\CcVault\.windsurf\workflows\ingest.md`

### 触发场景

用户把新 PDF 放入 `D:\CcVault\00_Raw\标准库\<region>\` 后调用。

### 执行步骤（Cascade 里发 `/ingest` 自动执行）

1. **入库前快照**：统计各 region 数量 + manifest pending 数
2. **干跑检查**（预估成本）：

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _daily_maintenance.py --dry-run
```

3. **预计成本 >¥10 时先问用户**
4. **真跑 pipeline**：

```powershell
.\.venv\Scripts\python.exe _daily_maintenance.py
```

自动 5 阶段：
- **A · Ingest**（OCR + Extract + Write）
- **B · Quality**（Cross-check + 假告警降级）
- **C · Navigation**（聚类 + 主题页 + 等价 + supersession）
- **D · Indices**（Graph + BM25）
- **E · 变化报告**

5. **读日志**：

```powershell
Get-ChildItem D:\CcVault\99_SystemScripts\auto_reg_index\logs\maintenance_*.log |
  Sort-Object LastWriteTime -Descending | Select-Object -First 1 |
  ForEach-Object { Get-Content $_.FullName -Tail 50 }
```

6. **抽查新 note 质量**：读 FM，检查 reg_id / title / region / status / publication_date 完整
7. **输出报告**

### 演示：完整报告格式

```markdown
# Ingest 完成报告

## 数字变化
- regulations: 1429 → 1437 (+8)
- cn: 448 → 452 (+4) / ece: 959 → 963 (+4)
- topics: 37 → 37 (无结构变化)

## 新入库（前 5 条）
1. GB 38031-2025 电动汽车用动力蓄电池安全要求 — confidence: high
2. ECE R155 Am3 Cyber security — confidence: high
...

## 需要关注
- 1 条 low-confidence（建议人工复核）
- 0 条 title 缺失

## 新发现的跨区域映射
- GB 38031-2025 ↔ ECE R100 Rev3 Am1 (relation=partial)

## 后续建议
- 查看 00_Dashboards/_Needs_Review.md 处理 low-confidence
```

### 成本参考

| 规模 | 成本 |
|---|---|
| 10 条新 PDF | ≈ ¥0.7 |
| 100 条 | ≈ ¥7 |
| 1000 条 | ≈ ¥70 |

---

## 5.2 `/add_note` — 手工新增一条法规

**完整定义**：`@D:\CcVault\.windsurf\workflows\add_note.md`

### 触发场景

用户有确定信息，不走 OCR pipeline，直接建 note。

### 执行步骤

1. **信息收集**：Cascade 主动询问缺失字段（reg_id / title / region / type / 日期 / supersedes / equivalent_to / topic）
2. **唯一性检查**：

```powershell
Get-ChildItem D:\CcVault\01_Wiki\regulations -Recurse -Filter "*.md" |
  ForEach-Object {
    if ((Get-Content $_ -First 5 | Out-String) -match "reg_id:\s*GB 38031-2025") {
      Write-Host "EXISTS: $($_.FullName)"
    }
  }
```

3. **写 note**（按 FM schema）：

```markdown
---
reg_id: GB 38031-2025
title: 电动汽车用动力蓄电池安全要求
title_en: Safety requirements for traction battery of electric vehicles
type: version
region: cn
status: active
publication_date: 2025-01-15
implementation_date: 2026-07-01
source_pdf: _ingest/cn/GB 38031-2025.pdf
summary: 规定电动汽车动力蓄电池安全要求与测试方法。
scope: 适用于 M1 / N1 类电动汽车用可充电储能系统。
supersedes:
  - GB 38031-2020
superseded_by: []
equivalent_to:
  - ref: ECE R100 Rev3
    relation: partial
    source: manually_added
keywords: [动力电池, 安全, 热失控]
cross_check_overall_confidence: high
cross_check_flags: []
tags:
  - reg/cn
  - type/version
  - status/verified
  - topic/hv_battery_ev
---

# GB 38031-2025 电动汽车用动力蓄电池安全要求

## 摘要
...

## 范围
...

## 关键要求
...
```

4. **建双向链**：若 supersedes 有值 → 老版本 FM 加 `status: superseded` + `superseded_by: [新 reg_id]`
5. **更新主题页** `04_Topics/hv_battery_ev - 电动车 · 动力电池 · 充电保护.md`
6. **重建索引**：

```powershell
.\.venv\Scripts\python.exe _build_supersession_chain.py
.\.venv\Scripts\python.exe _semantic_search.py --rebuild
```

### 禁止事项

- ❌ 不要在没确认的情况下改 `manifest.json`
- ❌ 不要创建 `01_Wiki/regulations/` 下除 region 外的子目录
- ❌ 不要省略必填 FM 字段（reg_id / title / region / status）
- ❌ 不要 hallucinate 日期或 title —— 不确定就问或留空

---

## 5.3 `/fix_classification` — 修正分类错误

**完整定义**：`@D:\CcVault\.windsurf\workflows\fix_classification.md`

### 触发场景

发现某条或某批 notes 的 `topic/<key>` tag 错了。典型：

- "GB 5135-2003 分到 fire_fighting_equipment 了，但它是消防车照明"
- "这 10 条 GB/T XXX 应该归 test_methods 不是 special_vehicles"

### 两种修法

#### Case A：规则级（推荐）

- 多条同类 notes 都错
- 有共同 pattern
- 未来还会有同类入库
- → **修 `_cluster_topics.py` 的 TOPICS 字典**

```python
TOPICS = {
    "fire_fighting_equipment": {
        "label": "消防器材",
        "keywords": [r"消防", r"灭火", r"喷水"],
        "exclude_keywords": [r"消防车.*照明", r"消防车.*信号"],  # 新增排除
    },
    "lighting_signaling": {
        "priority_reg_ids": [r"^GB 5135"],  # 新增优先匹配
    },
}
```

然后跑聚类：

```powershell
.\.venv\Scripts\python.exe _cluster_topics.py
```

#### Case B：单条异常

- 就 1-2 条错
- 没有可归纳的 pattern
- → **直接改 note tags + 加 MANUAL_OVERRIDES**

```python
MANUAL_OVERRIDES = {
    "GB 5135-2003": "lighting_signaling",  # 虽含"消防"但实为消防车照明
}
```

### 验证 + 重建

```powershell
.\.venv\Scripts\python.exe _cluster_topics.py
.\.venv\Scripts\python.exe _write_topic_pages.py
.\.venv\Scripts\python.exe _stage2_stats.py
.\.venv\Scripts\python.exe _semantic_search.py --rebuild
```

### 演示：修正报告

```markdown
# 分类修正报告

## 问题根因
- 场景：Case A 规则
- 根因：消防车照明被误分到 fire_fighting_equipment

## 修改内容
- 文件：_cluster_topics.py
- 规则变更：fire_fighting_equipment 新增 exclude_keywords

## 影响面
- 12 条 notes 重新分类
  - fire_fighting_equipment → lighting_signaling: 11 条
  - fire_fighting_equipment → special_vehicles: 1 条

## 验证
- 典型样本 GB 5135-2003 现在的 topic = lighting_signaling ✓
- BM25 索引已重建 ✓
```

---

## 5.4 `/weekly_check` — 每周健康巡检

**完整定义**：`@D:\CcVault\.windsurf\workflows\weekly_check.md`

### 触发场景

- "帮我巡检一下库"
- "这周库有啥异常吗"
- "健康度怎么样"

### 执行步骤（12 项检查）

1. **基础数字盘点**：

```powershell
Write-Host "regulations: $((Get-ChildItem D:\CcVault\01_Wiki\regulations -Recurse -Filter '*.md').Count)"
Get-ChildItem D:\CcVault\01_Wiki\regulations -Directory | ForEach-Object {
    $count = (Get-ChildItem $_.FullName -Recurse -Filter '*.md').Count
    Write-Host "  $($_.Name): $count"
}
```

2. **置信度分布**：

```powershell
.\.venv\Scripts\python.exe _stage2_stats.py
```

检查：high ≥ 70% / low ≤ 10% / verified ≥ 85%

3. **Manifest 一致性**：

```powershell
.\.venv\Scripts\python.exe _manifest_sync.py --check-only
```

4. **Wikilinks 失效**：扫所有 `[[...]]`，看 target 是否存在
5. **MOC 完整性**：每个 Topic 文件是否在 `_Topics MOC.md` 里有链接
6. **Orphan notes**：既不被引用也不在 MOC 里
7. **替代链健康**：`_build_supersession_chain.py --dry-run` 看 BROKEN / MISSING
8. **BM25 索引时效性**：最新 note 的 mtime > 索引 mtime → STALE
9. **Logs 滚动**：>30 天的 log 建议清理
10. **Staging 缓存大小**

### 演示：体检报告

```markdown
# CcVault 每周体检报告 — 2026-04-21

## 基础数字
- regulations: 1429 (cn: 448, ece: 959, 其他: 22)
- topics: 37
- dashboards: 10
- equivalence mappings: 95

## 数据质量
- high confidence: 70.8% (目标 ≥70%) ✓
- low confidence: 5.7% (目标 ≤10%) ✓
- verified tags: 87.4% (目标 ≥85%) ✓

## 一致性
- broken wikilinks: 3 (建议修复)
- MOC missing entries: 0
- supersession chain broken: 0
- BM25 index freshness: up-to-date ✓

## 维护建议
1. 修复 3 条 broken wikilinks
2. 处理 81 条 low-confidence notes

## 无异常项
- ✓ 置信度指标全绿
- ✓ 无 orphan notes
- ✓ staging 缓存合理（23 MB）
```

---

## 5.5 `/process_audits` — 批量处理人工反馈

**完整定义**：`@D:\CcVault\.windsurf\workflows\process_audits.md`

### 触发场景

`05_Audit/` 累积了若干 audit 条目。用户表达：

- "/process_audits"
- "处理一下 audit"
- "把 audit 里的问题都改了"

### Audit 条目结构

位置：`@D:\CcVault\05_Audit\<YYYY-MM-DD>_<brief>.md`

```yaml
---
target_file: 01_Wiki/regulations/cn/GB 4785-2019.md
target_reg_id: GB 4785-2019
target_section: ""
target_anchor: ""

severity: medium             # critical | high | medium | low
category: accuracy           # accuracy | completeness | classification | link | formatting
status: open                 # open | in_progress | resolved | wont_fix | duplicate

created: 2026-04-19T10:30:00
resolved: null
resolver: null

tags:
  - audit/open
  - audit/severity-medium
  - audit/category-accuracy
---

## Issue
<用户描述问题>

## Expected
<用户期望的修正>

## Resolution
<agent 处理后填>
```

### 处理流程

1. **扫所有 open**：按 severity 排（critical > high > medium > low）
2. **按 target_file 分组**：同 note 多 audit 一起改
3. **读 audit**：Issue + Expected + category
4. **评估 Issue 有效性**：如 agent 发现 Issue 描述不准 → 标 wont_fix
5. **修改 note**：按 category 执行
   - `accuracy` → 改 body 错误表述
   - `completeness` → 补充缺失内容
   - `classification` → 改 FM tags
   - `link` → 改 wikilinks / supersedes / equivalent_to
   - `formatting` → 调排版
6. **更新 audit 为 resolved**：
   - `status: resolved`
   - `resolved: 2026-04-19T14:30:00`
   - `resolver: cascade`
   - tags 中 `audit/open` → `audit/resolved`
   - Resolution 段填写实际改了什么
7. **索引重建**：若涉及 FM/supersedes/topics

```powershell
.\.venv\Scripts\python.exe _daily_maintenance.py --only-index
```

### 处理规则

**✅ 应该**：
- 严格按 Issue + Expected 改
- 保留原 Issue / Expected 段不动
- 同 note 多 audit 合并处理
- 改动后跑索引重建

**❌ 禁止**：
- 批量标 wont_fix 无理由
- 删 audit 文件（即使 duplicate 也保留）
- 修改 audit 原有的 Issue / Expected
- 跳过 critical / high
- 扩大修改范围

### 演示：处理报告

```markdown
# Audit 处理报告

## 总览
- 总处理: 12 条
- resolved: 10
- wont_fix: 1
- 跳过: 1

## 按严重度
- critical: 2/2 ✓
- high: 3/3 ✓
- medium: 5/5 ✓
- low: 2/2 ✓

## 涉及文件
- @D:\CcVault\01_Wiki\regulations\cn\GB 4785-2019.md — 3 audit 修复
- @D:\CcVault\01_Wiki\regulations\ece\ECE R48 Rev6.md — 2 audit 修复

## wont_fix 说明
- @05_Audit\2026-04-18_r48_scope.md — 原文确实是这样表达

## 索引重建
- BM25: ✓
- Supersession: ✓
- Topic clustering: 未涉及
```

### 成本估算

- 单条 audit：~¥0.01-0.05（DeepSeek）
- 一批 20 条：~¥0.5
- 重建索引：额外 25-40 秒但 0 成本

---

## 5.6 诊断工具（无 slash command）

### 5.6.1 `_scan_reg_id_anomalies.py` — 命名异常扫描

扫全库找 reg_id 命名异常。

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _scan_reg_id_anomalies.py
```

**分 6 类**：

| 类别 | 含义 |
|---|---|
| A | OCR 残留（如 `R013Hr4am1e` 未规范化） |
| B | 重复 reg_id（`_dup1` 后缀） |
| C | R13/R90 无 H 但 title 说乘用车 |
| D | 异名前缀（`UN R` / `R-H`） |
| E | 点号/破折号（`Rev.8` / `Rev.3/Amend.6`） |
| F | 数字零前缀 |

**示例输出**：

```
=== CcVault reg_id 异常扫描 ===
扫描目录：D:\CcVault\01_Wiki\regulations
总文件数：1429

[A] OCR 残留: 1 条
  R013Hr4am1e → 应为 ECE R13H Rev4 Am1

[B] 重复 reg_id: 57 条
  ECE R102 / R102_dup1
  ECE R108 / R108_dup1
  ...

[E] 点号破折号: 14 条
  ECE R13 Rev.8      → ECE R13 Rev8
  ECE R25 Rev.1      → ECE R25 Rev1
  ...
```

### 5.6.2 `_stage2_stats.py` — 质量分布

```powershell
.\.venv\Scripts\python.exe _stage2_stats.py
```

**示例输出**：

```
=== Stage 2 统计 ===
总数: 1429
high:    1011 (70.8%)
medium:   328 (22.9%)
low:       81 ( 5.7%)
unknown:    9 ( 0.6%)

按 flag 类型:
  title_mismatch:          12
  date_mismatch:            8
  scope_uncertain:         45
  ...
```

### 5.6.3 `_daily_maintenance.py` — 一键维护

```powershell
# 完整维护
.\.venv\Scripts\python.exe _daily_maintenance.py

# 只重建索引（手改 note 后）
.\.venv\Scripts\python.exe _daily_maintenance.py --only-index

# 干跑
.\.venv\Scripts\python.exe _daily_maintenance.py --dry-run
```

### 5.6.4 `_build_supersession_chain.py` — 版本链检查

```powershell
# 扫描 supersedes 并回填 superseded_by
.\.venv\Scripts\python.exe _build_supersession_chain.py

# 只检查不改
.\.venv\Scripts\python.exe _build_supersession_chain.py --dry-run
```

**示例输出**：

```
扫描了 1429 条 notes
- 发现 148 条有 supersedes 字段
- 93 个 orphan predecessors（老版本在 vault 里缺失）
- 新增/更新 superseded_by: 0 条
```

---

# 第 6 章 · Pipeline 内部（6 个 Stage）

Pipeline 把 `00_Raw/*.pdf` 变成 `01_Wiki/**/*.md`，分 6 个 stage（s0-s5）。每个 stage 可独立运行和断点续传。

**主入口**：`@D:\CcVault\99_SystemScripts\auto_reg_index\ingest.py`

**Manifest 状态机**（记录每份文件的处理进度）：

```
pending → ocr_done → extracted → verified/needs_review → written
                                                              ↓
         equivalence_linked → topic_summarized → graph_included
```

每份文件在 `manifest.json` 里维护这个状态，**任何阶段失败，下次 `resume` 从断点开始**。

---

## 6.1 Stage s0 · OCR（`@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s0_ocr.py`）

**输入**：`00_Raw/标准库/<region>/*.pdf`
**输出**：`.staging/<reg_id>/ocr.md`

**分层路由**（`@D:\CcVault\99_SystemScripts\auto_reg_index\ocr\router.py`）：

| 路由 | 工具 | 成本 | 场景 |
|---|---|---|---|
| **Tier 1** | pdfplumber | 免费 | 电子版 PDF（可选文字） |
| **Tier 2** | Baidu OCR | ¥0.04/页 | 扫描件（中英文） |
| **Tier 3** | MinerU CPU | 免费 | 复杂表格（慢但质量高） |

### 演示：单独跑 OCR

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe ingest.py run --stage 0
```

或对某个文件：

```powershell
.\.venv\Scripts\python.exe -m stages.s0_ocr --input "D:\CcVault\00_Raw\标准库\cn\GB 4785-2019.pdf"
```

---

## 6.2 Stage s1 · LLM 抽取（`@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s1_extract.py`）

**输入**：`.staging/<reg_id>/ocr.md`
**输出**：`.staging/<reg_id>/extracted.json`

**核心函数**：
- `extract_one_file()` — 单文件抽取入口
- `_canonicalize_reg_id()` — reg_id 规范化（第 2.1 节）
- `build_extract_prompt()` — prompt 构建

**LLM**：DeepSeek V3（成本最低，约 ¥0.02/PDF）

**Prompt 模板**：`@D:\CcVault\99_SystemScripts\auto_reg_index\prompts\extract.md`

**抽取字段**：

```json
{
  "reg_id": "GB 4785-2019",
  "title": "汽车及挂车外部照明和光信号装置的安装规定",
  "title_en": "Prescription for installation of lighting ...",
  "region": "cn",
  "type": "version",
  "status": "active",
  "publication_date": "2019-05-14",
  "implementation_date": "2020-01-01",
  "summary": "规定 M/N/O/L 类车辆外部照明...",
  "scope": "适用于 ...",
  "keywords": ["外部照明", "前照灯"],
  "supersedes": ["GB 4785-2007"],
  "references": ["GB 7258"]
}
```

### 演示：手动跑 s1

```powershell
.\.venv\Scripts\python.exe ingest.py run --stage 1
```

---

## 6.3 Stage s2 · Cross-check（`@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s2_cross_check.py`）

**输入**：`.staging/<reg_id>/extracted.json`
**输出**：落实 `cross_check_overall_confidence` 字段到 FM

**LLM**：Claude Sonnet 4.6（batch API，约 12 小时返回）

**Prompt 模板**：`@D:\CcVault\99_SystemScripts\auto_reg_index\prompts\cross_check.md`

**校对内容**：
- 标题 vs body 开头是否一致
- 日期是否在 body 中找得到证据
- 范围描述是否准确

**产出 flag**：

| Flag | 含义 |
|---|---|
| `title_mismatch` | 标题与正文不符 |
| `date_mismatch` | 日期与正文不符 |
| `scope_uncertain` | 范围存疑 |
| `type_uncertain` | 类型分类存疑 |
| `supersedes_uncertain` | supersedes 不确定 |

### 演示：重跑 cross-check

```powershell
.\.venv\Scripts\python.exe _run_cross_check.py --provider deepseek
```

---

## 6.4 Stage s3 · 跨区等价（`@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s3_equivalence.py`）

**输入**：`01_Wiki/regulations/**/*.md`
**输出**：`03_Equivalence/_Equivalence MOC.md` + 回写 FM.equivalent_to

**LLM**：Claude Opus 4.7（batch API，高难度推理）

**Prompt 模板**：`@D:\CcVault\99_SystemScripts\auto_reg_index\prompts\equivalence.md`

**推理方式**：按主题分组，让 Opus 在同主题内找跨区对应关系，输出 `{a_reg: ..., b_reg: ..., relation: ..., evidence: ...}`。

**relation 类型**：

| relation | 含义 |
|---|---|
| `identical` | 完全等同（GB 直接采标） |
| `equivalent` | 技术内容等效 |
| `modified` | 等同但有改动 |
| `adopts_from` | 单向采纳 |
| `aligned_with` | 部分对齐 |
| `partial` | 部分等价 |

### 演示：单独跑 s3

```powershell
.\.venv\Scripts\python.exe _extract_topic_equivalences.py
.\.venv\Scripts\python.exe _write_equivalence_page.py
.\.venv\Scripts\python.exe _apply_equivalences_to_notes.py
```

---

## 6.5 Stage s4 · 主题综述（`@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s4_topic_summary.py`）

**输入**：按 topic 聚合的 notes（由 `_cluster_topics.py` 预处理）
**输出**：`04_Topics/<topic>.md`

**LLM**：Claude Sonnet 4.6（batch API）

**Prompt 模板**：`@D:\CcVault\99_SystemScripts\auto_reg_index\prompts\topic_summary.md`

**主题页结构**（如 `@D:\CcVault\04_Topics\brakes - 制动系统.md`）：

```markdown
---
type: topic_index
topic_key: brakes
label: 制动系统
member_count: 84
---

# 制动系统

## 1. 领域概述
<2-3 段介绍主题范围、技术路径>

## 2. 跨区域速查
| 中国 | ECE | 美国 | 关系 |
|---|---|---|---|
| GB 21670-2008 | ECE R13H | FMVSS 135 | equivalent |
| GB 12676-2014 | ECE R13 | — | modified |
...

## 3. 完整索引

### China GB
- [[GB 12676-2014]] 商用车辆和挂车制动系统...
- [[GB 13594-2003]] 机动车和挂车防抱制动...
- [[GB 21670-2008]] 乘用车制动系统...

### ECE UN
- [[ECE R13]] M/N/O 类车辆制动
- [[ECE R13H]] 乘用车制动
...
```

### 演示：重生成主题页

```powershell
.\.venv\Scripts\python.exe _cluster_topics.py        # 重做聚类
.\.venv\Scripts\python.exe _write_topic_pages.py     # 写主题页
```

---

## 6.6 Stage s5 · GraphRAG 社区（`@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s5_graphrag.py`）

**输入**：`01_Wiki/regulations/**/*.md` + FM.supersedes/equivalent_to + body wikilinks
**输出**：`04_Topics/communities/community_000.md` – `community_032.md`

**3 步工程**：

### Step 1 · 构建关系图

```powershell
.\.venv\Scripts\python.exe _build_graph.py
```

**产物**：`.stage5/graph.json` + `.stage5/graph.graphml`

图结构：
- **节点**：1429 个法规 notes
- **边类型**：
  - `supersedes`（版本替代）
  - `equivalent_to`（跨区等价）
  - `references`（body 里的 wikilink）

### Step 2 · Louvain 社区检测

```powershell
.\.venv\Scripts\python.exe _graphrag_communities.py
```

用 python-louvain 做模块度优化，得到 **33 个社区**。

**产物**：`.stage5/communities.json`（每个社区的成员 + 核心节点）

### Step 3 · LLM 生成社区综述

```powershell
.\.venv\Scripts\python.exe _graphrag_summarize.py
```

**LLM**：Claude Opus 4.7 / DeepSeek V3
**Prompt 模板**：`@D:\CcVault\99_SystemScripts\auto_reg_index\prompts\graphrag_community.md`

每篇综述 800-1500 字，5 节：

1. 总体概述
2. 内部关系结构（含 mermaid 图）
3. 同类对比
4. 矛盾议题
5. 成员清单（wikilink）

### 演示：一键跑 s5

```powershell
.\.venv\Scripts\python.exe -m stages.s5_graphrag
```

### 查询时调用

```powershell
.\.venv\Scripts\python.exe _graphrag_search.py "乘用车制动系统要求"
```

---

## 6.7 Manifest 状态机管理

**位置**：`@D:\CcVault\99_SystemScripts\auto_reg_index\manifest.json`（1.4MB）

**状态查询**：

```powershell
.\.venv\Scripts\python.exe ingest.py status
```

**示例输出**：

```
=== Manifest 状态 ===
total: 1537 files

按状态分布:
  pending:              0
  ocr_done:             0
  extracted:            0
  verified:          1321
  needs_review:       108
  written:           1429
  equivalence_linked: 95
  topic_summarized:  1429
  graph_included:    1429

最近 5 个失败:
  (无)
```

**重试失败**：

```powershell
.\.venv\Scripts\python.exe ingest.py retry-failed
```

**成本报告**：

```powershell
.\.venv\Scripts\python.exe ingest.py cost-report
```

输出：

```
累计成本（按 provider）：
  deepseek    $8.55
  sonnet-4.6  $4.20
  opus-4.7    $1.58
总计：$14.33 / 预算 $180
```

---

## 6.8 CLI 命令总表

```
python ingest.py --help
```

| 命令 | 说明 |
|---|---|
| `sample --reg <REG_ID>` | 对某法规全流程（Phase 1 样板验证） |
| `ocr-only` | 只跑 OCR 层，不调 LLM |
| `run --stage <0-5>` | 只跑某一阶段 |
| `run --all` | 完整流水线 |
| `run --dry-run` | 不调真实 API，只 log 计划 |
| `resume` | 从断点续跑 |
| `status` | 打印 manifest 状态 |
| `cost-report` | 汇总 cost_log.jsonl |
| `retry-failed` | 重试所有 failed 文件 |

---

# 第 7 章 · MOC 导航层

**MOC (Maps of Content)** = Obsidian 里的导航枢纽，承担"目录"角色。

CcVault 有 **3 大 MOC**：

| MOC | 文件 | 内容 |
|---|---|---|
| 主题索引 | `@D:\CcVault\04_Topics\_Topics MOC.md` | 37 个主题导航 |
| 等价映射 | `@D:\CcVault\03_Equivalence\_Equivalence MOC.md` | 62 条 GB ↔ 外标 |
| 活面板 | `@D:\CcVault\00_Dashboards\_Dashboards MOC.md` | 10 个 Dataview 面板 |

---

## 7.1 Topics MOC

**文件**：`@D:\CcVault\04_Topics\_Topics MOC.md`

**作用**：给用户一个 37 主题的导航中心，每个主题一行 + 数量 + 链接到索引页。

**结构示例**：

```markdown
---
type: topic_moc
total_topics: 37
total_notes: 1429
---

# Topics MOC — 技术主题导航

## 按数量排序

| # | 主题 | Notes | 索引页 |
|---|---|---:|---|
| 1 | lighting_signaling | 262 | [[lighting_signaling - 照明与信号装置]] |
| 2 | tires_wheels | 115 | [[tires_wheels - 轮胎与车轮]] |
| 3 | emissions_exhaust | 100 | [[emissions_exhaust - 排放与燃料]] |
| 4 | crash_impact | 89 | [[crash_impact - 碰撞与被动安全]] |
| 5 | brakes | 84 | [[brakes - 制动系统]] |
| ... |

## 按类别分组

### 主动安全
- [[brakes - 制动系统]] (84)
- [[steering_suspension - 转向与悬挂]] (18)
- [[speed_control_speedometer - 车速 · 限速装置]] (11)

### 被动安全
- [[crash_impact - 碰撞与被动安全]] (89)
- [[restraints_airbags - 安全带与乘员约束]] (78)

### 动力与排放
- [[emissions_exhaust - 排放与燃料]] (100)
- [[fuel_lpg_cng - 燃料装置（液体 · 气体）]] (29)
- [[engine_power_performance - 发动机功率 · 性能测试]] (12)

...（9 个类别）
```

---

## 7.2 主题索引页（37 个）

**位置**：`@D:\CcVault\04_Topics\<topic_key> - <中文 label>.md`

**完整清单见 [附录 B](#附录-b--37-个-topic-全表)**。

每个主题页结构（以 `brakes - 制动系统.md` 为例）：

```markdown
---
type: topic_index
topic_key: brakes
label: 制动系统
member_count: 84
updated: 2026-04-19
tags:
  - topic/brakes
  - type/topic_index
---

# 制动系统

## 1. 领域概述
制动系统是汽车主动安全的核心...
涵盖 ABS / ESC / EBS 系统、液压/气压制动...

## 2. 跨区域速查

| 中国标准 | ECE | 美国 FMVSS | 关系 |
|---|---|---|---|
| [[GB 21670-2008]] | [[ECE R13H]] | FMVSS 135 | equivalent |
| [[GB 12676-2014]] | [[ECE R13]] | FMVSS 105, 121 | modified |
| [[GB 13594-2003]] | [[ECE R13]] Annex 13 | FMVSS 135 Annex | aligned_with |

## 3. 完整索引

### 中国 GB（21 条）
- [[GB 12676-2014]] 商用车辆和挂车制动系统技术要求及试验方法
- [[GB 13594-2003]] 机动车和挂车防抱制动性能和试验方法
- [[GB 21670-2008]] 乘用车制动系统技术要求及试验方法
...

### ECE（63 条）
- [[ECE R13]] 有关 M、N 和 O 类车辆制动...
- [[ECE R13H]] 关于乘用车制动认证...
- [[ECE R13 Rev8 Am1]] ...
...
```

---

## 7.3 Equivalence MOC

**文件**：`@D:\CcVault\03_Equivalence\_Equivalence MOC.md`

**作用**：62 条跨区域等价映射的汇总表，按技术主题分组。

**结构示例**：

```markdown
---
type: equivalence_moc
mappings: 62
---

# 跨区域等价映射 MOC

## 照明与信号

| 中国 | ECE | 关系 | 备注 |
|---|---|---|---|
| [[GB 4785-2019]] | [[ECE R48 Rev6]] | equivalent | 外部照明安装 |
| [[GB 4660-2020]] | [[ECE R19 Rev8]] | modified | 前雾灯 |
| [[GB 4599-2007]] | [[ECE R98]] | equivalent | 前照灯 |

## 制动

| 中国 | ECE | 关系 | 备注 |
|---|---|---|---|
| [[GB 21670-2008]] | [[ECE R13H]] | equivalent | 乘用车制动 |
...
```

### 演示：生成 Equivalence MOC

```powershell
.\.venv\Scripts\python.exe _extract_topic_equivalences.py
.\.venv\Scripts\python.exe _write_equivalence_page.py
```

---

## 7.4 GraphRAG 社区 MOC

**文件**：`@D:\CcVault\04_Topics\communities\_Communities MOC.md`

**作用**：33 个社区的总览，按 label 分类。

**结构示例**：

```markdown
---
type: community_moc
total_communities: 33
---

# GraphRAG Communities MOC

## 按成员数排序

| # | Label | 成员 | 边 | 主区域 | 主题 |
|---|---|---:|---:|---|---|
| 003 | 排放限值 / 燃料消耗 / 替代燃料 | 18 | 20 | cn | emissions_exhaust |
| 011 | 制动系统 / ESC / ABS / 制动液 | 14 | 18 | ece | brakes |
| 008 | 照明 / 前照灯 / 转向信号灯 | 14 | 17 | ece | lighting_signaling |
...
```

---

# 第 8 章 · Dashboards（10 个活面板）

**位置**：`@D:\CcVault\00_Dashboards\`

**入口**：`@D:\CcVault\00_Dashboards\_Dashboards MOC.md`

**前提**：需要 Obsidian 装 **Dataview 插件**（必装）。

---

## 8.1 `_Needs_Review.md` — 低置信度清单

**内容**：所有 `cross_check_overall_confidence == low` 或 `tags: status/needs-review` 的 notes。

**Dataview 查询**：

```dataview
TABLE 
  status as 状态,
  cross_check_overall_confidence as 置信度,
  cross_check_flags as 标记,
  topic as 主题
FROM "01_Wiki/regulations"
WHERE cross_check_overall_confidence = "low" OR contains(tags, "status/needs-review")
SORT cross_check_overall_confidence ASC, reg_id ASC
```

### 演示：在 Obsidian 里打开

```
Ctrl-O → 搜索 "_Needs_Review" → 打开
```

面板会自动刷新当前所有低置信度 notes。

---

## 8.2 `_By_Region_Latest.md` — 各区域最近 20 条

**内容**：每个 region 最近发布的 20 条。

**用途**：扫新规发布。

---

## 8.3 `_Emissions_Watch.md` — 排放跟踪

**内容**：`topic/emissions_exhaust` 下的所有 notes，按日期排序。

---

## 8.4 `_EV_BEV_Watch.md` — 新能源/电池

**内容**：`topic/hv_battery_ev` 下的所有 notes。

---

## 8.5 `_Supersession_Chains.md` — 替代链

**内容**：所有有 `superseded_by` 或 `supersedes` 字段的 notes，按版本链展示。

**Dataview 查询**：

```dataview
TABLE
  supersedes as 替代,
  superseded_by as 被替代,
  publication_date as 发布日期
FROM "01_Wiki/regulations"
WHERE supersedes OR superseded_by
SORT publication_date DESC
```

---

## 8.6 `_Cross_Region_Matrix.md` — 跨区域对标矩阵

**内容**：GB ↔ ECE ↔ FMVSS 对应矩阵。

---

## 8.7 `_Recent_Amendments.md` — 最近 3 月 Amendment

**内容**：`type: amendment` 且 `publication_date` 在 90 天内的 notes。

---

## 8.8 `_High_Confidence_Index.md` — 高质量索引

**内容**：`cross_check_overall_confidence == high` 的 notes。

---

## 8.9 `_Graph_Insights.md` — 关系网分析（静态）

**内容**：从 `.stage5/graph.json` 导出的网络统计（中心性 / 连通性）。

---

## 8.10 `_Semantic_Search.md` — BM25 使用说明

**内容**：使用 `_semantic_search.py` 的命令示例（非动态，是教程页）。

---

# 第 9 章 · 故障排查

## 9.1 HTTP 服务 422 错误

**现象**：Obsidian Copilot 对话返回 422 Unprocessable Entity。

**原因**：Pydantic schema 不接受 Copilot 发送的 `content: [{type: text, text: ...}]` 格式（多模态 OpenAI 格式）。

**修复**（已完成）：`@D:\CcVault\99_SystemScripts\auto_reg_index\agent\server.py` 的 `Message.content` 字段改为 `Any`，并在 handler 里加 `_content_to_text()` 规范化。

**验证**：

```powershell
curl -X POST http://127.0.0.1:7777/v1/chat/completions `
  -H "Content-Type: application/json" `
  -d '{"messages":[{"role":"user","content":[{"type":"text","text":"hi"}]}],"model":"ccvault-agent"}'
```

返回 200 + 正常答案即修复成功。

---

## 9.2 MCP 不生效

**现象**：Cascade 里发"用 ccvault 查..."不触发 tool call。

**排查**：

1. **配置文件路径对**：`%USERPROFILE%\.codeium\windsurf\mcp_config.json`
2. **Python 路径对**：`.venv/Scripts/python.exe` 存在
3. **完全退出客户端**：任务栏托盘 → 右键 Quit（不只是关窗口）
4. **查日志**：Cascade 设置 → MCP → 看 stderr 输出

**测试 MCP server 是否能启**：

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _agent_mcp.py
# 应该看到 stderr: "注册 12 个 tools"
# Ctrl-C 退出
```

---

## 9.3 ingestion 失败

### 9.3.1 某份 PDF 一直 pending

**检查**：`@D:\CcVault\99_SystemScripts\auto_reg_index\logs\ocr_errors.log`

**可能原因**：
- PDF 损坏
- OCR 超时（大文件）
- Baidu API 额度用完

**处理**：

```powershell
# 重试该文件
.\.venv\Scripts\python.exe ingest.py retry-failed
```

### 9.3.2 Sonnet batch 12h 还没回

**检查**：Anthropic console 看 batch status。

**处理**：
- batch 一般 12-24h 返回
- 超过 48h 建议重新提交

### 9.3.3 `confidence: low` 占比 > 30%

**原因**：Prompt 不够明确，LLM 抽取质量差。

**处理**：
1. 调 `@D:\CcVault\99_SystemScripts\auto_reg_index\prompts\extract.md`
2. 重跑：`python ingest.py retry-failed`

---

## 9.4 BM25 索引过期

**现象**：搜新 note 找不到，或搜老 note 排序异常。

**判断**：

```powershell
$indexFile = "D:\CcVault\99_SystemScripts\auto_reg_index\.stage5\bm25_index.pkl"
$lastNote = Get-ChildItem D:\CcVault\01_Wiki\regulations -Recurse -Filter '*.md' |
    Sort-Object LastWriteTime -Descending | Select-Object -First 1

if ($lastNote.LastWriteTime -gt (Get-Item $indexFile).LastWriteTime) {
    Write-Host "STALE: 最新 note ($($lastNote.Name)) 比索引新"
}
```

**修复**：

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe _semantic_search.py --rebuild
```

---

## 9.5 reg_id 命名异常（R13H 家族等）

**现象**：新入库的 note reg_id 带奇怪 OCR 残留（`R013Hr4am1e`）。

**根因**：s1_extract 的 reg_id 正则缺失 H 后缀支持（**已修复**，2026-04-21）。

**历史数据修复**：

```powershell
.\.venv\Scripts\python.exe _scan_reg_id_anomalies.py
# 看 7 类异常分布，按需手动或脚本清理
```

详见 memory 里的《CcVault 完整审计报告》。

---

## 9.6 版本链 `get_supersession_chain` 返回空

**现象**：调 `get_supersession_chain("GB 4785-2019")` 返回无版本链。

**原因**：note body 里写了"本标准代替 GB 4785-2007"但 FM 的 `supersedes` 字段没抽到。

**诊断**：

```powershell
grep "本标准代替" D:\CcVault\01_Wiki\regulations\cn\*.md
# 返回 62 条命中
```

**修复**（待实现）：需要新脚本 `_backfill_supersedes_from_body.py` 从 body 正则回填。

---

## 9.7 manifest.json 损坏

**症状**：`ingest.py status` 报错 / `resume` 死循环。

**处理**：

```powershell
# 不要手改！从 git 恢复
git -C D:\CcVault log --oneline -- 99_SystemScripts/auto_reg_index/manifest.json
git -C D:\CcVault checkout <commit> -- 99_SystemScripts/auto_reg_index/manifest.json
```

---

# 第 10 章 · 开发扩展

## 10.1 代码目录结构

```
99_SystemScripts/auto_reg_index/
├── ingest.py                 # 主 CLI 入口
├── manifest.py               # Manifest 状态机管理
├── config.yaml               # Pipeline 行为配置
├── .env / .env.template      # API keys
│
├── stages/                   # 6 个 pipeline 阶段
│   ├── s0_ocr.py
│   ├── s1_extract.py         # LLM 抽取 + _canonicalize_reg_id
│   ├── s2_cross_check.py
│   ├── s3_equivalence.py
│   ├── s4_topic_summary.py
│   └── s5_graphrag.py
│
├── writers/                  # 输出到 Obsidian
│   └── obsidian_writer.py    # FM 构建 + 文件名生成
│
├── ocr/                      # OCR 分层
│   ├── router.py             # Tier 路由
│   ├── pdfplumber_extractor.py
│   ├── baidu_ocr.py
│   └── mineru_extractor.py
│
├── llm/                      # LLM 客户端（统一接口）
│   ├── base.py
│   ├── deepseek_client.py
│   ├── claude_client.py
│   └── prompts.py
│
├── prompts/                  # Prompt 模板 .md
│   ├── extract.md
│   ├── cross_check.md
│   ├── equivalence.md
│   ├── topic_summary.md
│   └── graphrag_community.md
│
├── agent/                    # ★ ReAct Agent 层
│   ├── __init__.py
│   ├── agent.py              # build_agent / run_once
│   ├── cli.py                # REPL + rich
│   ├── server.py             # FastAPI OpenAI 兼容
│   ├── mcp_server.py         # MCP stdio
│   └── tools/                # 12 个工具
│       ├── __init__.py       # ALL_TOOLS 汇总
│       ├── _shared.py        # FM 加载、reg_id 匹配、索引加载
│       ├── search.py         # 3 个检索 tool
│       ├── stats.py          # 4 个统计 tool
│       ├── structure.py      # 4 个结构 tool
│       └── audit.py          # 1 个审计 tool
│
├── _agent_chat.py            # → agent.cli.main()
├── _agent_server.py          # → agent.server.main()
├── _agent_mcp.py             # → agent.mcp_server.main()
│
├── cache/                    # LLM 响应缓存（gitignored）
├── logs/                     # 运行日志（gitignored）
├── .staging/                 # 中间产物（gitignored）
├── .stage3/ .stage4/ .stage5/  # 阶段产物
│
└── _*.py                     # 30+ 辅助脚本（详见附录 E）
```

## 10.2 扩展新 Agent 工具

想加新 tool，三步：

### Step 1：在对应 category 文件写函数

```python
# agent/tools/search.py（或 stats/structure/audit）
from langchain.tools import tool

@tool
def my_new_tool(param1: str, param2: int = 10) -> str:
    """描述这个 tool 干什么（LLM 会读这里的 docstring 决定何时调用）。
    
    Args:
        param1: 第一个参数
        param2: 第二个参数，默认 10
    """
    # 实现...
    return "tool 输出的字符串（≤4000 字符）"
```

### Step 2：加到 category 列表

```python
# 文件末尾
SEARCH_TOOLS = [..., my_new_tool]
```

### Step 3：如果是新 category

```python
# agent/tools/__init__.py
from .my_category import MY_CATEGORY_TOOLS
ALL_TOOLS = SEARCH_TOOLS + STATS_TOOLS + ... + MY_CATEGORY_TOOLS
```

**无需改 `agent.py` 或 `cli.py`**。HTTP / MCP / CLI 全部自动暴露新工具。

---

## 10.3 扩展新主题 (topic)

### Step 1：改 `_cluster_topics.py`

```python
TOPICS = {
    # ... 已有的 37 个
    "new_topic_key": {
        "label": "新主题中文名",
        "keywords": [r"关键词1", r"关键词2"],
        "priority_reg_ids": [r"^GB 12345"],  # 可选
        "exclude_keywords": [r"排除词"],  # 可选
    },
}
```

### Step 2：重跑聚类

```powershell
.\.venv\Scripts\python.exe _cluster_topics.py
.\.venv\Scripts\python.exe _write_topic_pages.py
```

### Step 3：更新 MOC

编辑 `@D:\CcVault\04_Topics\_Topics MOC.md` 加新主题一行。

---

## 10.4 扩展新区域 (region)

### Step 1：创建目录

```powershell
mkdir D:\CcVault\01_Wiki\regulations\<new_region>
mkdir D:\CcVault\00_Raw\标准库\<new_region>
```

### Step 2：更新 taxonomy

改 `@D:\CcVault\02_Schema\02_taxonomy.md` 加新 region。

### Step 3：扩展 FM 规则

若新 region 有特殊 reg_id 规则，改 `_canonicalize_reg_id()`（在 `@D:\CcVault\99_SystemScripts\auto_reg_index\stages\s1_extract.py`）。

### Step 4：跑一次 ingestion 测试

```powershell
.\.venv\Scripts\python.exe ingest.py sample --reg <测试 reg_id>
```

---

## 10.5 扩展新 LLM provider

### Step 1：实现 `llm/base.py` 接口

```python
# llm/new_provider_client.py
from .base import LLMClient

class NewProviderClient(LLMClient):
    def chat(self, messages: list, **kwargs) -> str:
        # 实现...
```

### Step 2：注册到工厂

```python
# llm/__init__.py
from .new_provider_client import NewProviderClient

def get_client(provider: str) -> LLMClient:
    if provider == "new_provider":
        return NewProviderClient()
    ...
```

### Step 3：更新 `config.yaml`

```yaml
llm:
  s1_extract:
    provider: new_provider   # 替换
    model: <model-name>
```

---

## 10.6 编码约定

- **所有 LLM 调用**必须走 `llm/` 下统一客户端（不要裸调 openai / anthropic）
- **所有文件路径**用 `pathlib.Path`（不要字符串拼接）
- **所有阶段可独立测试**：`python -m stages.s1_extract --input <path>`
- **禁止在 pipeline 中修改** `00_Raw/` 下任何文件
- **禁止 commit `.env`**（已在 `.gitignore`）
- **Tool 返回 string**，限制 ≤4000 字符（避免 LLM context 爆炸）
- **Import 必须在文件顶部**，不要在函数内或文件中间

---

## 10.7 测试指南

### 单元测试（无 LLM）

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index
.\.venv\Scripts\python.exe -m pytest tests/ -v
```

### 集成测试（Sample 法规）

```powershell
# 对 GB 4785 跑全流程
.\.venv\Scripts\python.exe ingest.py sample --reg "GB 4785"
# 约 15 分钟，消耗 <$1 + ¥5
```

### Agent 冒烟测试

```powershell
.\.venv\Scripts\python.exe _agent_chat.py "vault 里有多少法规" -v
```

---

# 附录 A · Frontmatter 字段完整表

权威定义：`@D:\CcVault\02_Schema\03_frontmatter_schema.md`

## 必填字段

| 字段 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `reg_id` | str | `GB 4785-2019` | 规范化唯一编号 |
| `title` | str | `汽车及挂车外部照明...` | 中文/本地语标题 |
| `region` | str | `cn` | 区域代码（见附录 C） |
| `status` | str | `active` | 状态（见 2.5） |
| `type` | str | `version` | regulation / version / amendment |

## 强烈建议字段

| 字段 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `title_en` | str | `Prescription for...` | 英文标题 |
| `publication_date` | date | `2019-05-14` | 发布日期 |
| `implementation_date` | date | `2020-01-01` | 实施日期 |
| `source_pdf` | str | `_ingest/cn/GB 4785-2019.pdf` | 源 PDF 相对路径 |
| `cross_check_overall_confidence` | str | `high` | high/medium/low/unknown |
| `cross_check_flags` | list | `[title_mismatch]` | 校对时发现的 flag |

## 关系字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `supersedes` | list[str] | 替代的老版本 reg_id |
| `superseded_by` | list[str] | 脚本自动回填，**手工不填** |
| `equivalent_to` | list[dict] | `{ref, relation, source}` |
| `references` | list[str] | body 里引用的 reg_id（可选） |

## 内容字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `summary` | str | 2-3 句摘要 |
| `scope` | str | 适用范围 |
| `keywords` | list[str] | 关键词（用于 BM25） |

## 分类标签

| 字段 | 类型 | 示例 |
|---|---|---|
| `tags` | list[str] | `[reg/cn, type/version, status/verified, topic/brakes]` |

## 标签命名空间

| 命名空间 | 示例 | 来源 |
|---|---|---|
| `reg/<region>` | `reg/cn`, `reg/ece` | 来自 region |
| `type/<type>` | `type/regulation`, `type/version`, `type/amendment` | 来自 type |
| `status/<q>` | `status/verified`, `status/needs-review` | 来自 confidence |
| `topic/<key>` | `topic/brakes`, `topic/lighting_signaling` | 来自聚类 |

---

# 附录 B · 37 个 Topic 全表

文件位置：`@D:\CcVault\04_Topics\<topic_key> - <label>.md`

| # | topic_key | label | 数量 |
|---|---|---|---:|
| 1 | `lighting_signaling` | 照明与信号装置 | 262 |
| 2 | `tires_wheels` | 轮胎与车轮 | 115 |
| 3 | `emissions_exhaust` | 排放与燃料 | 100 |
| 4 | `crash_impact` | 碰撞与被动安全 | 89 |
| 5 | `brakes` | 制动系统 | 84 |
| 6 | `restraints_airbags` | 安全带与乘员约束 | 78 |
| 7 | `noise` | 噪声 | 68 |
| 8 | `special_vehicles` | 特种 · 危险车辆 | 55 |
| 9 | `visibility_glazing` | 视野 · 玻璃 · 雨刮 | 53 |
| 10 | `anti_theft_security` | 防盗与安全防护 | 53 |
| 11 | `test_methods` | 试验方法 · 测量规程 | 47 |
| 12 | `fire_fighting_equipment` | 消防器材 · 灭火系统 | 44 |
| 13 | `operator_controls_indicators` | 操纵件 · 指示器位置 | 39 |
| 14 | `fuel_lpg_cng` | 燃料装置（液体 · 气体） | 29 |
| 15 | `motorcycle` | 摩托车 · L 类 | 27 |
| 16 | `body_markings` | 车身标识 · 反光标志 | 26 |
| 17 | `interior_protrusions` | 内部凸出物 · 内饰 | 24 |
| 18 | `adas_driver_assist` | ADAS · 驾驶员辅助系统 | 23 |
| 19 | `electronics_emc` | 电气电子与 EMC | 23 |
| 20 | `commercial_operations` | 营运 · 商用车管理 | 22 |
| 21 | `recycling_reuse` | 回收 · 再制造 · 禁用物质 | 20 |
| 22 | `bus_coach` | 客车 · 公交车 | 18 |
| 23 | `type_approval_general` | 总体型式认证 · 通用要求 | 18 |
| 24 | `steering_suspension` | 转向与悬挂 | 18 |
| 25 | `hv_battery_ev` | 电动车 · 动力电池 · 充电保护 | 15 |
| 26 | `engine_power_performance` | 发动机功率 · 性能测试 | 12 |
| 27 | `identification` | 车辆识别 · 标记 | 12 |
| 28 | `speed_control_speedometer` | 车速 · 限速装置 | 11 |
| 29 | `lubricants_fluids` | 润滑油 · 工作液 | 9 |
| 30 | `doors_mechanisms` | 门锁 · 铰链 · 座椅机构 | 9 |
| 31 | `dimensions_weights` | 尺寸 · 质量 · 类别 | 9 |
| 32 | `overview_directory` | 目录 · 体系概览 | 8 |
| 33 | `energy_labeling` | 能耗 · 油耗标识 | 6 |
| 34 | `reference_material` | 参考资料（非法规） | 6 |
| 35 | `out_of_scope` | 非汽车法规 · 越界 | 6 |
| 36 | `trailer_coupling` | 挂车 · 联结装置 | 4 |
| 37 | `certification_admin` | 强制认证 · 管理制度 | 3 |
| 38 | `misc` | 其他 · 未归类 | 5 |

（数量来自 `.stage4/cluster_assignment.json` 聚类结果）

---

# 附录 C · 16 个 Region 全表

| region | 数量 | 全称 | 命名空间 |
|---|---:|---|---|
| `ece` | 959 | 联合国欧洲经济委员会 (UN ECE) | `01_Wiki/regulations/ece/` |
| `cn` | 448 | 中国 (GB / GB/T) | `01_Wiki/regulations/cn/` |
| `eu` | 5 | 欧盟指令 | `01_Wiki/regulations/eu/` |
| `ru-eaeu` | 3 | 俄罗斯 / 欧亚经济联盟 | `01_Wiki/regulations/ru-eaeu/` |
| `cl` | 2 | 智利 | `01_Wiki/regulations/cl/` |
| `us` | 2 | 美国 (FMVSS) | `01_Wiki/regulations/us/` |
| `au` | 1 | 澳大利亚 (ADR) | `01_Wiki/regulations/au/` |
| `br` | 1 | 巴西 | `01_Wiki/regulations/br/` |
| `gcc` | 1 | 海湾合作委员会 | `01_Wiki/regulations/gcc/` |
| `id` | 1 | 印度尼西亚 | `01_Wiki/regulations/id/` |
| `jp` | 1 | 日本（安保基準） | `01_Wiki/regulations/jp/` |
| `kr` | 1 | 韩国 (KMVSS) | `01_Wiki/regulations/kr/` |
| `my` | 1 | 马来西亚 | `01_Wiki/regulations/my/` |
| `sa` | 1 | 沙特阿拉伯 | `01_Wiki/regulations/sa/` |
| `th` | 1 | 泰国 | `01_Wiki/regulations/th/` |
| `za` | 1 | 南非 | `01_Wiki/regulations/za/` |

---

# 附录 D · 12 个 Agent 工具 API 参考

**所有工具都返回字符串**，限制 ≤4000 字符。通用 import：

```python
from agent.tools import ALL_TOOLS  # 12 个工具的列表
from agent.tools.search import search_regulations_bm25, search_communities_graphrag, read_regulation
from agent.tools.stats import describe_vault, count_regulations, stats_by_field, list_recent_changes
from agent.tools.structure import get_equivalence, get_supersession_chain, get_community, list_by_topic
from agent.tools.audit import list_needs_review
```

## D.1 检索类（3）

### `search_regulations_bm25`

```python
search_regulations_bm25(
    query: str,
    region: Optional[str] = None,
    topic: Optional[str] = None,
    limit: int = 10,
) -> str
```

### `search_communities_graphrag`

```python
search_communities_graphrag(
    query: str,
    topk_communities: int = 3,
    topk_members: int = 5,
) -> str
```

### `read_regulation`

```python
read_regulation(
    reg_id: str,
    body_chars: int = 2000,
) -> str
```

## D.2 统计类（4）

### `describe_vault`

```python
describe_vault() -> str
# 无参数
```

### `count_regulations`

```python
count_regulations(
    region: Optional[str] = None,
    topic: Optional[str] = None,
    status: Optional[str] = None,
    year_from: Optional[int] = None,
    year_to: Optional[int] = None,
    confidence: Optional[str] = None,
) -> str
```

### `stats_by_field`

```python
stats_by_field(
    field: str,   # region/topic/status/year/confidence/source_type
    top_n: int = 30,
) -> str
```

### `list_recent_changes`

```python
list_recent_changes(
    days: int = 7,
    limit: int = 20,
) -> str
```

## D.3 结构类（4）

### `get_equivalence`

```python
get_equivalence(reg_id: str) -> str
```

### `get_supersession_chain`

```python
get_supersession_chain(reg_id: str) -> str
```

### `get_community`

```python
get_community(community_id: int) -> str  # 0-32
```

### `list_by_topic`

```python
list_by_topic(
    topic: str,
    limit: int = 50,
) -> str
```

## D.4 审计类（1）

### `list_needs_review`

```python
list_needs_review(
    limit: int = 20,
    confidence: str = "low",
) -> str
```

---

# 附录 E · 50+ 脚本清单

位置：`@D:\CcVault\99_SystemScripts\auto_reg_index\`

## E.1 主入口（3 个）

| 脚本 | 用途 |
|---|---|
| `ingest.py` | 完整 pipeline CLI |
| `_daily_maintenance.py` | 一键维护（最常用） |
| `manifest.py` | Manifest 状态机 |

## E.2 Agent 层（3 个）

| 脚本 | 用途 |
|---|---|
| `_agent_chat.py` | CLI 模式（REPL + 单次） |
| `_agent_server.py` | HTTP OpenAI 兼容服务 |
| `_agent_mcp.py` | MCP stdio 服务 |

## E.3 Pipeline 阶段（6 个）

| 脚本 | 阶段 |
|---|---|
| `stages/s0_ocr.py` | OCR |
| `stages/s1_extract.py` | LLM 抽取 |
| `stages/s2_cross_check.py` | 校对 |
| `stages/s3_equivalence.py` | 跨区等价 |
| `stages/s4_topic_summary.py` | 主题综述 |
| `stages/s5_graphrag.py` | GraphRAG 社区 |

## E.4 数据建设（10 个）

| 脚本 | 用途 |
|---|---|
| `_backfill_titles.py` | 补 FM 缺失字段（title/date/status） |
| `_run_cross_check.py` | 质量复核 |
| `_reclassify_false_mismatches.py` | 降级假告警 |
| `_recheck_low_confidence.py` | 低置信度重审 |
| `_fix_existing_notes.py` | 批量修 FM |
| `_migrate_non_automotive.py` | 迁移非法规到 02_Wiki |
| `_retry_baidu_ocr.py` | 重试 Baidu OCR 失败 |
| `_find_scanned.py` | 识别扫描件 PDF |
| `_post_ocr_report.py` | OCR 后处理报告 |
| `_qc_full.py` `_qc_sample.py` `_qc_content.py` `_qc_baidu_sample.py` | QC 抽样检查 |

## E.5 导航层（5 个）

| 脚本 | 用途 |
|---|---|
| `_cluster_topics.py` | 主题聚类（37 topics） |
| `_write_topic_pages.py` | 生成 37 个主题页 |
| `_extract_topic_equivalences.py` | 从主题页抽等价对 |
| `_write_equivalence_page.py` | 生成 Equivalence MOC |
| `_apply_equivalences_to_notes.py` | 回写到 FM.equivalent_to |

## E.6 关系层（2 个）

| 脚本 | 用途 |
|---|---|
| `_build_supersession_chain.py` | 版本链双向维护 |
| `_build_graph.py` | 构建关系图（.stage5/graph.json） |

## E.7 GraphRAG（4 个）

| 脚本 | 用途 |
|---|---|
| `_graphrag_communities.py` | Louvain 聚类 |
| `_graphrag_summarize.py` | LLM 生成社区综述 |
| `_graphrag_search.py` | 层级检索（查询时用） |
| `_graph_analytics.py` | 网络分析（中心性等） |

## E.8 检索（1 个）

| 脚本 | 用途 |
|---|---|
| `_semantic_search.py` | BM25 检索 + 索引重建 |

## E.9 诊断（6 个）

| 脚本 | 用途 |
|---|---|
| `_scan_reg_id_anomalies.py` | 命名异常扫描（7 类） |
| `_stage2_stats.py` | 置信度分布统计 |
| `_topic_digest.py` | 主题覆盖统计 |
| `_manifest_sync.py` | manifest 同步检查 |
| `_manifest_sync_check.py` | 同步问题告警 |
| `_show_positions.py` | 某 reg_id 在 manifest 里的位置 |

## E.10 成本 / 预算（2 个）

| 脚本 | 用途 |
|---|---|
| `_baidu_cost_estimate.py` | 预估 Baidu OCR 费用 |
| `_generate_supervisor_report.py` | 生成监督报告 |

## E.11 辅助（5 个）

| 脚本 | 用途 |
|---|---|
| `_probe_claude.py` | 测试 Claude API |
| `_collect_equivalences.py` | 收集所有 equivalence 数据 |
| `_gemini_setup.ps1` | Gemini 环境初始化 |

---

# 附录 F · 命令速查卡

## F.1 日常查询（最常用）

```powershell
cd D:\CcVault\99_SystemScripts\auto_reg_index

# 【★】自然语言问一切
.\.venv\Scripts\python.exe _agent_chat.py "你的问题"

# 【★】REPL 连续对话
.\.venv\Scripts\python.exe _agent_chat.py

# BM25 关键词搜
.\.venv\Scripts\python.exe _semantic_search.py "前照灯 LED" --region cn

# GraphRAG 层级搜
.\.venv\Scripts\python.exe _graphrag_search.py "乘用车制动系统要求"
```

## F.2 维护

```powershell
# 一键维护
.\.venv\Scripts\python.exe _daily_maintenance.py

# 只重建索引
.\.venv\Scripts\python.exe _daily_maintenance.py --only-index

# 干跑
.\.venv\Scripts\python.exe _daily_maintenance.py --dry-run

# 统计
.\.venv\Scripts\python.exe _stage2_stats.py

# 命名异常扫描
.\.venv\Scripts\python.exe _scan_reg_id_anomalies.py
```

## F.3 Agent 服务

```powershell
# HTTP 服务（Obsidian Copilot 用）
.\.venv\Scripts\python.exe _agent_server.py

# 允许局域网访问
.\.venv\Scripts\python.exe _agent_server.py --host 0.0.0.0 --port 7777
```

## F.4 单阶段 pipeline

```powershell
# 某法规走全流程
.\.venv\Scripts\python.exe ingest.py sample --reg "GB 4785"

# 只跑某阶段
.\.venv\Scripts\python.exe ingest.py run --stage 1

# 状态查询
.\.venv\Scripts\python.exe ingest.py status

# 成本报告
.\.venv\Scripts\python.exe ingest.py cost-report
```

## F.5 Workflow（Cascade/Windsurf 里发送）

| 命令 | 触发场景 |
|---|---|
| `/ingest` | 新 PDF 入库 |
| `/add_note` | 手工新增法规 |
| `/fix_classification` | 修正分类错误 |
| `/weekly_check` | 每周健康巡检 |
| `/process_audits` | 批量处理 audits |

## F.6 搜索 vault

```powershell
# 按 reg_id 精确找
Get-ChildItem D:\CcVault\01_Wiki\regulations -Recurse -Filter "GB 4785*.md"

# 读 FM
Get-Content "D:\CcVault\01_Wiki\regulations\cn\GB 4785-2019.md" -First 30

# 按关键词找 body
Get-ChildItem D:\CcVault\01_Wiki\regulations -Recurse -Filter "*.md" |
  Select-String "前照灯" | Select-Object -First 10
```

---

## 🎯 结语

本文档涵盖 CcVault **所有**能力：

- ✅ **12 个工具** × 4 种调用方式 = 48 种用法
- ✅ **5 个 Workflow** × 每个完整步骤 + 示例
- ✅ **6 个 Pipeline Stage** × 实现代码路径
- ✅ **3 大 MOC** × 结构 + 生成方式
- ✅ **10 个 Dashboard** × 各自用途
- ✅ **50+ 脚本** × 一句话说明
- ✅ **37 个 topic** × 完整清单
- ✅ **16 个 region** × 完整清单

### 维护本文档

本文档应在以下情况更新：
- 增加新 Agent 工具 → 更新第 3 章 + 附录 D
- 增加新 Workflow → 更新第 5 章
- 增加新 Pipeline 阶段 → 更新第 6 章
- 增加新 topic/region → 更新附录 B/C

### 相关文档

- `@D:\CcVault\CLAUDE.md` — AI Agent 规则（必读）
- `@D:\CcVault\_使用说明.md` — 用户完整手册（另一视角）
- `@D:\CcVault\_INDEX.md` — 全局主索引
- `@D:\CcVault\_CHANGELOG.md` — 变更历史

---

**最后更新**：2026-04-21
**维护者**：Cascade
**版本**：v1.0
